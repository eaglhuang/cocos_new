# 3KLife 資料管理中心 — 架構規格書

**文件類型**: 正式規格書  
**建立日期**: 2026-04-06  
**整合日期**: 2026-04-06  
**最後進度盤點**: 2026-04-08（技術總監）  
**來源分析**: 技術總監架構規劃 + 里程碑解決方案  
**版本**: v1.5

---

## §0 進度總覽（2026-04-07 盤點）

> 依據 `docs/tasks/tasks-dc.json` 實際交付物驗證，總計 **37/37 任務完成（100%）**。

| 里程碑 | 任務數 | 完成 | 未完成 | 狀態 |
|--------|--------|------|--------|------|
| M0 稀有度系統 | 3 | 3 | 0 | ✅ 完成 |
| M1 資料管線基礎 | 7 | 7 | 0 | ✅ 完成（200 武將已入庫） |
| M2 分層儲存架構 | 6 | 6 | 0 | ✅ 完成 |
| M3 血統正規化 | 5 | 5 | 0 | ✅ 完成 |
| M4 壓縮＋序列化 | 3 | 3 | 0 | ✅ 完成 |
| M5 增量同步協議 | 5 | 5 | 0 | ⚠️ Phase 1 完成；Phase 2/3 待實作 |
| M6 完整覆蓋＋增長控管 | 6 | 6 | 0 | ✅ 完成 |
| M7 武將資料編輯工具 | 2 | 2 | 0 | ✅ 完成 |

### 未完成清單（0 個 open 任務）

> 目前資料中心任務卡已全數完成；M7 改以 repo 內 smoke 驗收腳本作為可重複驗收門檻。

### 2026-04-07 新增工具（M1 追加交付物）

| 工具 | 說明 |
|------|------|
| `tools_node/build-generals-runtime.js` | master → runtime 一鍵編譯管線；merge 與 Editor 儲存時自動觸發 |
| `tools_node/audit-core-generals-quality.js` | 核心 50 名武將品質稽核（模板命中率、血脈傳聞泛用率、rarityTier 分布） |
| `tools_node/curate-core-generals.js` | 前 10 數值人工校準 + 前 50 故事/血脈傳聞一次性策展腳本 |
| `tools_node/report-datacenter-gap.js` | DC 任務缺口報告產生器，輸出至 `docs/tasks/data-center-gap-report.md` |
| `tools_node/validate-general-data-tooling.js` | DC-7 smoke 驗收：驗證 Editor Extension 與 Runtime Debug Panel 的搜尋、回寫、validate/build、DEV guard 與 DataCatalog 即時反映 |

> npm scripts：`build:generals-runtime`、`curate:core-generals`、`report:dc-gap`、`audit:core50`、`check:general-data-tooling`

### 建議下一步優先順序

1. **前 50 武將 historicalAnecdote 補寫** — 7 筆過短（< 80 字），風格尚為 Wiki 體，建議補成碑文式趣聞
2. **武將校準擴充至第 51–100 位** — 套用相同 `TOP_BASELINE` 模式，推進 epic 梯隊數值一致性
3. **將 `tools_node/validate-general-data-tooling.js` 納入日常驗收** — 建議在資料編輯或 pipeline 調整後固定執行一次
4. **視需要補 Editor 實機錄屏或截圖證據** — 目前已有 repo 內 smoke 驗收，可再加人工操作證據作為附檔

---

## TL;DR

為 3KLife 規劃完整的「大規模武將資料管理 + 血統傳承 + 離線/同步」技術架構，涵蓋：史料自動收集、本地分層儲存、資料正規化、壓縮策略、增量同步協議。分 6 個里程碑交付。

## 0、整合說明

- 本文件已正式整合原始規劃稿 `docs/資料管理中心技術架構與里程碑.md` 的全部有效內容。
- 自 2026-04-06 起，資料中心相關架構、里程碑與技術決策，均以本文件作為單一真相來源。
- 舊規劃稿完成整併後可自文件樹移除，後續不得再以平行文件持續維護相同主題。

---

## M1 深入設計（2026-04-06 追加）

### M1-A：ingest-generals-wiki.js 設計

- 資料來源：維基百科三國志人物列表 + koei-wiki 光榮三國志系列資料
- 雙來源策略：Wiki 取史料背景，光榮 Wiki 取遊戲化數值參考
- 欄位映射：25 個核心欄位 + 6 大屬性 + 適性 + 故事條
- AI prompt 模板：風格統一的「歷史趣聞」「血脈傳聞」「故事條」生成

### M1-B：General Data Editor 設計

- 雙形態工具：Cocos Editor Extension（不進包）+ Runtime Debug Panel（DEV_MODE flag 控制）
- Editor Extension 採 menu + panel 形態，直接讀寫 `generals.json`
- Runtime Panel 採 `StyleCheckPanel` 同類模式，支援 UI 即時調參 + 搜尋 DSL
- 打包排除：Editor Extension 天生不進包；Runtime 工具用 `CC_DEV` macro guard

### M1-C：Skills 體系設計

- 全部改走本機 Copilot Skills（零 API 成本）
- 新增 3 個 skills：
  1. `general-story-writer`：故事條 / 野史 / 血脈傳聞批次生成
  2. `general-balance-tuner`：六維數值平衡計算 + EP 重算 + 適性調參 + 稀有度自動計算
  3. `general-data-pipeline`：整合爬取 → 合併 → 驗證 → 匯出的端到端管線
- 所有 skills 參照既有 `dalle3-image-gen` / `ui-brief-generator` 的 frontmatter 格式
- Skills 純粹在 Copilot 對話中執行，不依賴外部 API

### M1-D：稀有度系統設計

- 兩層稀有度模型：核心 `rarityTier`（平衡）+ 商業 `commercial band`（展示 / 包裝）
- 核心五級稀有度：N / R / SR / SSR / UR，作為戰力、適性、EP 與資料稽核的唯一真相來源
- 商業七色分層：白 / 綠 / 藍 / 紫 / 金 / 紅 / 彩，作為卡面、招牌角色、活動 / 限定 / 旗艦包裝的衍生顯示層
- 角色分類欄位：`civilian` / `general` / `famed` / `mythical` / `titled`
- 自動判定演算法：雙軸制（`maxStat + avg5`），取較高 tier
- 商業分層不得反寫核心平衡；女將商業補正只允許作用於商業層，不直接改原始屬性
- 少數招牌角色可用白名單包成金卡；活動卡 / 限定卡 / 旗艦卡可與純戰力分開
- 門檻值改為 JSON 可配置，不寫死在程式碼
- `GeneralDetailRarityTier` 已擴展加入 `mythic`（UR）
- `characterCategory` 納入 `GeneralConfig` 與資料管線

---

## 一、現況盤點

### 1.1 已有基礎

| 項目 | 現況 |
|------|------|
| `generals.json` | 目前 10 位初始武將，每位 ~3-8KB |
| `GeneralConfig` 介面 | 40-50 欄位含 `Ancestors_JSON`（14 人矩陣） |
| Data Schema 規格書 | 完整定義 Root Save Schema + Action_Records + HMAC 鏈 |
| 血統矩陣 | 3 代 14 人，因子用 gene_id 引用共用字典 |
| Server 規劃 | NoSQL/JSON MySQL，save_snapshots 為核心表 |
| 熱更新 | Web(MD5 Cache) / Native(AssetsManager) 已定義 |
| 離線同步 | HMAC Hash Chain + Event Replay 已設計 |

### 1.2 關鍵缺口（尚未解決）

1. **史料資料管線**：幾百位武將的背景、野史、故事條尚無自動收集/結構化工具
2. **分層載入策略**：全量 JSON 在中後期 350+ 位武將 × 5KB = 1.75MB+，未分包
3. **血統正規化**：`Ancestors_JSON` 嵌套在每位武將內，跨世代有重複祖先
4. **增量同步協議**：只有全量 Data + Action_Records 設計，無 delta sync
5. **壓縮/序列化**：JSON 純文字無二進制壓縮
6. **資料增長控管**：戰鬥日誌、培育會話等高頻資料無 TTL/歸檔策略

---

## 二、技術架構設計

### 2.1 史料自動收集管線（Data Ingestion Pipeline）

**工具鏈：**
- `tools_node/ingest-generals-wiki.js`：爬取公開三國 Wiki（維基百科三國志人物列表、三國誌遊戲資料庫）→ 結構化 JSON
- `tools_node/ingest-generals-ai.js`：使用 AI 對每位武將生成「歷史趣聞」「血脈傳聞」故事條 → 審核 → 入庫
- `assets/resources/data/master/` 目錄結構：

```
generals-base.json         # 基礎屬性（ID/名字/勢力/初始數值/適性）
generals-lore.json         # 歷史背景與野史（大文字量，延遲載入）
generals-stories.json      # 故事條模板（插槽式敘事）
gene-dictionary.json       # 因子共用字典（100 種）
bloodline-templates.json   # 虛擬祖先模板庫
troop-definitions.json     # 兵種定義
```

**自動化流程：**

```
Wiki/公開資料 → ingest-generals-wiki.js → raw JSON
                     ↓
              人工審核 + 補正
                     ↓
AI 生成 → ingest-generals-ai.js → 故事條/野史
                     ↓
              merge-generals-master.js → master/*.json
                     ↓
              validate-generals-data.js → 品質檢查
```

**資料來源策略（雙來源）：**
- 維基百科三國志人物列表：取史料背景
- koei-wiki 光榮三國志系列資料：取遊戲化數值參考

**AI 生成欄位映射：**
- 25 個核心欄位 + 6 大屬性 + 適性 + 故事條
- AI prompt 模板：風格統一的「歷史趣聞」「血脈傳聞」「故事條」生成

### 2.2 本地分層儲存架構

**三層分檔策略（解決 JSON 過大問題）：**

| 層級 | 檔案 | 載入時機 | 大小估算（中期） |
|------|------|---------|----------------|
| L0 索引層 | `generals-index.json` | App 啟動 | ~50KB（350 武將 × 150B） |
| L1 活躍層 | `generals-active.json` | 進入 Lobby | ~200KB（當前陣容 30 位 × 5KB + 經濟/時間） |
| L2 倉庫層 | `generals-storage-{page}.json` | 開倉庫/圖鑑 | 每頁 ~100KB（20 位 × 5KB） |
| L3 故事層 | `generals-lore-{id}.json` | 點開人物詳情 | 每位 ~2-5KB |
| L4 歷史層 | `battle-records-{season}.json` | 查看戰績 | 每季 ~50KB |
| L5 血統層 | `bloodline-tree-{uid}.json` | 查看血統樹 | 每位 ~1-3KB |

Unity 對照：L0=Addressables catalog、L1=首屏 Preload Group、L2=Lazy Load Group、L3-L5=Remote Bundle

**關鍵技術：**
- 使用 IndexedDB (Web) / SQLite (Native) 取代純 JSON 檔案
- `DataStorageAdapter` 抽象層：統一 API，底層自動選擇 IndexedDB 或 SQLite
- 分頁查詢：倉庫採 page cursor，不一次載入全部

### 2.3 血統資料正規化

**問題描述：**  
如果張飛的孫子和趙雲的孫女結緣，他們子嗣的 `Ancestors_JSON` 會重複儲存張飛/趙雲的因子資料，造成大量冗餘。

**正規化方案：**

```
┌─────────────────────┐     ┌──────────────────────┐
│  Person Registry    │     │  Bloodline Link      │
│  (主檔)             │     │  (關係圖)             │
├─────────────────────┤     ├──────────────────────┤
│ uid: string (PK)    │←────│ child_uid: string    │
│ template_id: string │     │ parent_uid: string   │
│ name: string        │     │ relation: F|M        │
│ gene_refs: string[] │     │ generation: number   │
│ ep_base: number     │     └──────────────────────┘
│ faction: string     │
│ is_virtual: boolean │
└─────────────────────┘
```

- 每個人（含虛擬祖先）只有一筆 `Person Registry` 記錄
- `Ancestors_JSON` 改為 `ancestor_chain: string[]`（14 個 uid），查表取因子
- **節省量估算**：同一祖先被 N 個子嗣引用時，從 N × 200B 降為 N × 16B (uid) + 1 × 200B (主檔)
- 中期 350 位武將 × 14 祖先中平均 30% 重複 → 省 ~30% 血統資料量

### 2.4 壓縮策略

| 層級 | 技術 | 壓縮率 | 適用場景 |
|------|------|--------|---------|
| JSON 欄位縮寫 | 因子用 `["B","STR_05",3]` 取代完整物件 | ~50% | 全部 JSON |
| MessagePack | 二進制序列化替代 JSON.stringify | ~40% | 存檔快照 + 同步傳輸 |
| pako (zlib) | gzip 壓縮 MessagePack 結果 | ~60-70% | 同步上傳/下載 |
| 增量 Delta | 只傳差異欄位 | ~80-95% | 每次同步 |

**中期資料量估算：**
- 全量 JSON：~2MB
- MessagePack：~1.2MB
- + gzip：~400-600KB
- Delta sync（日常）：~5-20KB / 次

### 2.5 增量同步協議（Delta Sync）

**現有設計限制**：全量 `Data` + `Action_Records` → Server，中期 2MB 每次全量上傳不合理

**三階段演進方案：**

```
Phase 1（MVP）: 全量同步
  Client → { Data, Action_Records } → Server
  Server → Event Replay → 覆蓋存檔

Phase 2（正式版）: Delta 同步
  Client → { delta_patch, Action_Records, data_hash } → Server
  Server → 驗證 hash → apply delta → confirm

Phase 3（多裝置）: CRDT-lite
  Server 維護 vector clock
  多裝置各自離線 → 上傳時 merge
  衝突解決：Action_Records 的 timestamp 排序 + Server 仲裁
```

**Delta Patch 格式（JSON Patch RFC 6902）：**

```json
{
  "version": "1.3.0",
  "base_hash": "abc123...",
  "patches": [
    { "op": "replace", "path": "/Generals/G001/Stats/STR", "value": 95 },
    { "op": "add", "path": "/Generals/G350", "value": { "..." : "..." } },
    { "op": "remove", "path": "/Battle_Records/S120" }
  ]
}
```

### 2.6 資料增長控管策略

| 資料類型 | 控管策略 | TTL |
|---------|---------|-----|
| 戰鬥日誌 | 最近 100 場完整 → 之前按季摘要 → 2 年前刪除 | 100 場 / 2 年 |
| 培育會話 | 完成後僅保留摘要（最終數值 + 關鍵事件） | 完成即壓縮 |
| 史實武將（已故） | 轉為 Spirit_Card 快照，保留基本檔 / 血脈索引 / 復活資格；完整主體移冷歸檔層 | 永久保留 |
| 動態後裔（存活） | 熱/冷分層；非活躍資料季結封存，只保留目前玩法必要欄位常駐 | 每季滾動整理 |
| 動態後裔（已故） | 轉為 Spirit_Card + 血脈摘要；完整主體移待刪區，達條件後批次刪除 | 待刪 1–4 季 |
| 已滅絕支系 | 壓縮為 FamilyBranchSummary，只保留族譜節點 / 基因摘要 / 代表事件 | 支系滅絕即收斂 |
| 故事條 | LRU 快取，最多 20 位武將的完整故事常駐記憶體 | LRU 20 |

**長局單機資料生命周期策略（2026-04-08 追加）**

- **A 類：史實武將** — 視為世界觀常駐資產。即使死亡，也只做「熱資料釋放」，不做硬刪除；保留 `SpiritCard`、基本身分檔、血脈索引與可復活資格。
- **B 類：動態後裔（玩家 / AI 自行繁衍）** — 視為可回收資料。存活時只保留目前玩法需要的 hot fields；故事、歷程、戰績、完整祖先快照全部走 cold archive / season rollup。
- **C 類：動態後裔（已故）** — 死亡後立刻產生 `SpiritCard`，只保留最小必要集：`uid`、父母鏈、`gene_refs`、出生/死亡年、勢力、`rarityTier`/`characterCategory`、`ep_snapshot`、英靈卡索引。完整主體進入 pending-delete quarantine。
- **D 類：不可再復活的後裔** — 若同時滿足「非史實」、「無復活池資格」、「無存活後代依賴」、「無未結算事件 / 任務 / 合約引用」，即可由批次 GC 從待刪區清除完整主體，只留下血脈摘要或支系摘要。
- **AI 陣營額外限制** — AI 後裔必須有生育配額、世代上限或人口預算；超額時優先走支系壓縮與 seed 重建策略，而不是持續常駐全量武將物件。
- **建議批次節奏** — 每季做一次 rollup（摘要化），每年做一次 branch compaction（支系壓縮），每 4 季做一次 pending-delete 清掃；玩家可在設定中保留「最近 X 季家族史」供回看。
- **補充收斂方案**：
  - 非關鍵 AI 後裔可只存 lineage seed + delta，必要時再重建，而不是永久存完整物件。
  - 已滅絕支系只保留 `FamilyBranchSummary`，不再保留每一代完整行為日誌。
  - 戰績、社交、事件日志一律 seasonal rollup，不允許綁死在後裔主體上無限累積。
  - 設定全域存檔配額警戒線；超標時先降級為摘要模式，再執行延後清掃，而不是讓存檔無限制膨脹。

---

## 三、稀有度與角色分類系統

### 3.1 五級稀有度

| 等級 | 代號 | 說明 |
|------|------|------|
| N | common | 一般武將；核心平衡最低層 |
| R | rare | 稀有武將；基礎可培養層 |
| SR | epic | 精銳武將；中堅戰力層 |
| SSR | legendary | 傳說武將；高端戰力層 |
| UR | mythic | 神話武將；僅神話 / 手動指定 |

- `rarityTier` 是**核心平衡 tier**，只負責戰力、EP、適性標準、資料盤點與系統解鎖條件。
- 核心 tier 不等於卡面框色，也不直接等於轉蛋包裝等級。
- UI 顏色可引用 tier 色作為輔助視覺，但不得把商業包裝結果反向回寫 `rarityTier`。

**商業化 7 色分層（Commercial Band）**

| Band | 說明 | 主要用途 |
|------|------|---------|
| white | 白 / 基礎 | 基礎卡面與低商業包裝 |
| green | 綠 / 進階 | 初階稀有展示 |
| blue | 藍 / 精良 | 主流量產卡面 |
| purple | 紫 / 菁英 | 主力商業高稀有層 |
| gold | 金 / 招牌 | 招牌角色與主視覺卡池 |
| red | 紅 / 限定級 | 活動、聯動、期間限定 |
| rainbow | 彩 / 旗艦級 | 旗艦包裝、最高商業展示 |

- `commercial band` 是**衍生展示層**，由 `rarityTier`、`characterCategory` 與商業規則共同計算，不作為戰力判定真相。
- 商業 band 允許和核心 tier 解耦，用來支援女將商業呈現、招牌金卡、活動卡、限定卡、旗艦卡等包裝需求。
- 設計原則：**戰力平衡不要被商業包裝污染；商業包裝也不要回寫原始五維。**

### 3.2 角色分類欄位

- `civilian`：平民
- `general`：一般將領
- `famed`：名將
- `mythical`：神話人物
- `titled`：帝王/封號（未來擴充）

### 3.3 稀有度自動判定演算法（雙軸制）

- **雙軸**：maxStat（最高單項屬性）+ avg5（六維去 luk 後平均值）
- 取兩軸各自判定後的**較高 tier**
- `mythic` 等級須明確設定 `characterCategory: 'mythical'` 或手動指定 `rarityTier`

**門檻值（可 JSON 設定，不寫死）：**

| Tier | maxStat 門檻 | avg5 門檻 |
|------|------------|---------|
| legendary | ≥ 95 | ≥ 80 |
| epic | ≥ 80 | ≥ 65 |
| rare | ≥ 65 | ≥ 50 |
| common | < 65 | < 50 |

**計算公式：**

```text
Axis A（偏科天才）
  maxStat = max(str, int, lea, pol, cha)

Axis B（綜合實力）
  avg5 = average(str, int, lea, pol, cha)

coreTier = max(resolveByMaxStat(maxStat), resolveByAvg5(avg5))
```

**商業分層計算（獨立於核心 tier）：**

```text
commercialScore =
  baseScoreByTier(coreTier)
  + categoryBonus(characterCategory)
  + genderScoreBonus(gender)
  + thresholdBonus(maxStat, avg5, commercialCha)
  + aptitudeBonus(A/S count)

commercialBand = scoreBands(commercialScore)
commercialBand = applyFeaturedBandWhitelist(commercialBand, id)
commercialBand = applyPremiumFallback(commercialBand)
commercialBand = applyGenderCap(commercialBand, gender)
commercialBand = applyBandAvg5Floor(commercialBand, gender, avg5)
```

**現行商業規則結論：**

- 女將魅力補正只存在商業層：`commercialCha = round(sqrt(cha) * 10)`；不改寫原始 `cha`。
- 招牌金卡採 `featuredBandIds` 白名單；不是所有 `legendary` 都自動變金卡。
- `red` / `rainbow` 屬於活動、限定、旗艦等商業包裝層，不能拿來定義核心戰力。
- 男將 `purple` 需額外滿足 `avg5` 下限，避免商業高稀有層數量膨脹。
- 原則上只維護一套主資料：`rarityTier` / `characterCategory` / 原始五維；商業 band 一律由規則衍生。

**公式層平衡設計原則（2026-04-08）：**

- 遊戲平衡不應直接截斷基本屬性（str/int/lea/pol/cha）的數值上限，應透過公式層實現差距收斂。
- **戰鬥公式**：以加權組合（`STR×0.7 + LEA×0.3`）或指數壓縮（`stat^0.7`）降低頂端差距，不修改原始屬性。
- **生產 / 政務公式**（已實作基礎版）：`adminOutput = floor(sqrt(pol) × multiplier × aptitudeBonus)`、`diplomacyOutput = floor(sqrt(cha) × multiplier × aptitudeBonus)`；以開根號曲線收斂高端差距。
- **適性加乘**（已實作 helper，待串接到完整戰場 / 內政流程）：`FormulaSystem.aptitudeMultiplier(grade)`，目前規則為 S=1.2x、A=1.1x、B=1.0x、C=0.9x、D=0.8x。
- 此原則與商業層分離原則相同：基本屬性是「歷史詮釋的語意」；公式層是「遊戲體驗的平衡」，兩者職責分離，互不污染。
- 實作位置：`assets/scripts/core/systems/FormulaSystem.ts`（現有 `calculateGeneralAttack`、`calculateAdminOutput`、`calculateDiplomacyOutput`、`aptitudeMultiplier`）；`assets/scripts/core/models/GeneralUnit.ts` 已補 `pol` / `cha` 正式欄位。

**代碼位置**：`assets/scripts/ui/components/GeneralDetailOverviewMapper.ts` → `resolveRarityTier()`

### 3.4 整合位置

- `assets/scripts/core/models/GeneralUnit.ts`：`GeneralDetailRarityTier`、`characterCategory` 欄位
- `assets/resources/data/rarity-thresholds.json`：核心 tier 門檻與 `commercialRarity` 規則來源
- `tools_node/audit-generals-commercial-balance.js`：核心 tier / 商業 band / category / aptitude 一致性稽核
- `general-balance-tuner` skill：自動分類入庫
- `general-data-pipeline` skill：入庫時自動打 tag

---

## 四、Copilot Skills 體系

所有資料管線工具改走本機 Copilot Skills（零 API 成本），3 個 skill 已建立：

| Skill 名稱 | 用途 | 檔案 |
|-----------|------|------|
| `general-story-writer` | 故事條/野史/血脈傳聞批次生成 | `.github/skills/general-story-writer/SKILL.md` |
| `general-balance-tuner` | 六維數值平衡計算 + EP 重算 + 適性調參 + 稀有度自動計算 | `.github/skills/general-balance-tuner/SKILL.md` |
| `general-data-pipeline` | 整合爬取→合併→驗證→匯出的端到端管線 | `.github/skills/general-data-pipeline/SKILL.md` |

Skills 在 Copilot 對話中執行，不依賴外部 API。格式參照 `dalle3-image-gen` / `ui-brief-generator` frontmatter 規範。

---

## 五、武將資料編輯工具

### 5.1 雙形態工具設計

| 形態 | 技術 | 適用場景 |
|------|------|---------|
| Cocos Editor Extension | menu + panel，直接讀寫 `generals-base.json` → 自動 rebuild runtime | 開發期大批量編輯 |
| Runtime Debug Panel | `DEV` from `cc/env`（build-time 排除），UI 即時調參 + 搜尋 DSL | 執行期快速驗證 |

- **打包排除**：Editor Extension 天生不進包；Runtime Panel 用 `import { DEV } from 'cc/env'` build-time guard（發布包 DEV=false 觸發 dead-code elimination）
- Runtime Panel 走 `StyleCheckPanel` 同類模式（已有參照）

### 5.2 DC-7-0001 實作進度（2026-04-07）

| 檔案 | 狀態 |
|------|------|
| `extensions/general-data-editor/package.json` | ✅ 完成（manifest、menu、messages 齊全） |
| `extensions/general-data-editor/src/main.js` | ✅ 完成（openPanel / validateData / updateGeneralFields / 自動 rebuild runtime） |
| `extensions/general-data-editor/src/search.js` | ✅ 完成（matchesGeneralQuery 搜尋 DSL） |
| `extensions/general-data-editor/panels/default/index.js` | ✅ 完成（HTML table 列表 + 欄位編輯表單 + IPC 呼叫） |
| `tools_node/validate-general-data-tooling.js` | ✅ 完成（Extension manifest / IPC / 搜尋 DSL / 欄位回寫 / validate + runtime build smoke 驗收） |

> 注意：Cocos Editor Extension 腳本在 Electron Node.js 環境執行（CommonJS，不走 Cocos TypeScript 編譯鏈），交付物格式為 `.js` 符合 Cocos 3.8 Extension 規範。原規格書 deliverables 列名 `.ts` 為筆誤，已修正。

**驗收結果**：repo 內 smoke 驗收已通過：`node tools_node/validate-general-data-tooling.js` / `npm run check:general-data-tooling` 可驗證搜尋 → 修改 → 儲存 → validate → runtime rebuild 流程。

### 5.3 DC-7-0002 實作進度（2026-04-07）

| 功能 | 狀態 |
|------|------|
| 搜尋 DSL（uid / 名稱 / faction / tier 篩選） | ✅ 完成 |
| 即時欄位修改並反映到 DataCatalog | ✅ 完成（applySelectedChanges → catalog.updateEntry） |
| build-time CC_DEV 排除（`import { DEV } from 'cc/env'`） | ✅ 完成（2026-04-07） |
| repo 內 smoke 驗收（DEV guard / 搜尋 / 選取 / updateEntry） | ✅ 完成（2026-04-07） |

**驗收結果**：`node tools_node/validate-general-data-tooling.js` 已覆蓋 `DEV=false` destroy、搜尋 DSL、欄位載入、即時數值修改與 `DataCatalog.updateEntry` 反映；`get_errors` 顯示 0 TS errors。

---

## 六、里程碑規劃

### M1：資料管線基礎（Data Pipeline Foundation）

**目標**：建立武將主資料收集 + 結構化工具鏈

**依賴**：無

**交付物**：
- `tools_node/ingest-generals-wiki.js` — 公開資料爬取 + 結構化
- `tools_node/ingest-generals-ai.js` — AI 生成故事條/野史
- `tools_node/merge-generals-master.js` — 合併 + 去重 + 驗證
- `tools_node/validate-generals-data.js` — 資料品質檢查（必填欄位/型別/範圍）
- `assets/resources/data/master/` 目錄結構建立
- 首批 50 位核心武將完整資料落地
- `docs/系統規格書/武將資料管線規格書.md`

**驗收指標**：`node tools_node/validate-generals-data.js` 零 error

**M1 細節說明（2026-04-06 追加）：**

- 全部改走本機 Copilot Skills（零 API 成本）
- 雙來源策略：Wiki 取史料背景，光榮 Wiki 取遊戲化數值參考
- 所有 skills 參照既有 dalle3-image-gen / ui-brief-generator 的 frontmatter 格式
- Skills 純粹在 Copilot 對話中執行，不需要外部 API

---

### M2：分層儲存架構（Layered Storage Architecture）

**目標**：告別單一大 JSON，實現按需載入

**依賴**：M1（master 資料格式確定）

**交付物**：
- `DataStorageAdapter` 抽象類（IndexedDB / SQLite 雙實作）
- `DataCatalog` 索引層（L0 快速查表）
- `DataPageLoader` 分頁載入器（L1-L5 按需載入）
- `SchemaMigration` 服務（schema 版本控制 + 漸進式遷移登錄）
- `ResourceManager` 擴充 `loadPagedData()` API
- `MemoryManager` 擴充 data scope（`'master-active'`, `'master-storage'`, `'lore'`）
- 效能基準測試：啟動時間、記憶體峰值、分頁載入延遲

**驗收指標**：350 位武將模擬資料，啟動時 L0 < 100ms，倉庫分頁 < 50ms

> **⚠️ 實作誠實備注（2026-04-08）**：
> - `SqliteAdapter` 目前所有方法仍為 TODO stub；**Native 平台本地儲存尚不可用**。正式 Native 支持排入 P-deferred，需評估 Cocos jsb bridge 或 better-sqlite3-wasm。
> - `DataPageLoader` 目前在 adapter 未初始化時仍以 `generals.json` 全量載入 + 記憶體過濾（已加 `_jsonCache` 避免重複 load）。真正 DB-layer 按需分頁需等 adapter 完成後切換至 `adapter.query()` 路徑（L1/L2 程式碼已留好條件分支）。
> - `generals-base.json` 已加入 `schemaVersion: 1`；`SchemaMigration.ts` 服務已建立，提供漸進式遷移登錄機制。

---

### M3：血統正規化（Bloodline Normalization）

**目標**：Person Registry + 關係圖取代嵌套 Ancestors_JSON

**依賴**：M2（儲存層就緒）

**交付物**：
- `PersonRegistry` 資料表 + TypeScript 介面
- `BloodlineGraph` 服務（查詢祖先鏈、計算 EP）
- `Ancestors_JSON` → `ancestor_chain` 遷移腳本
- `BloodlineTreePanel` 從 registry 渲染（不再讀嵌套 JSON）
- `validate-bloodline-integrity.js` — 確保圖完整性

**驗收指標**：5 代 500 位武將 + 2000 虛擬祖先，資料量 < 舊方案 70%

---

### M4：壓縮 + 序列化（Compression & Serialization）

**目標**：存檔體積和同步頻寬降至合理範圍

**依賴**：M3（正規化後才壓縮才有效）

**交付物**：
- MessagePack 序列化整合（`@msgpack/msgpack`）
- pako gzip 壓縮整合
- `SaveSerializer` 服務（serialize/deserialize + compress/decompress）
- JSON 欄位縮寫映射表（壓縮模式/展開模式雙向轉換）
- 效能基準：中期存檔全量 < 500KB compressed

**驗收指標**：2MB JSON → < 500KB compressed，解壓 < 50ms

---

### M5：增量同步協議（Delta Sync Protocol）

**目標**：從全量同步升級為差異同步

**依賴**：M4（序列化層就緒）

**交付物**：
- `DeltaPatchBuilder` 服務（生成 JSON Patch RFC 6902）
- `SyncManager` 改造：delta mode + fallback 全量
- Server API spec：`POST /sync/delta` + `POST /sync/full`
- `Action_Records` 壓縮上傳（batch + gzip）
- Network 狀態偵測 + 自動重試 + 斷點續傳

**驗收指標**：日常同步 < 20KB、失敗重試 3 次、斷線恢復無資料遺失

> **⚠️ 實作誠實備注（2026-04-08）**：
> - `DeltaPatchBuilder` 和 `SyncManager.syncDelta()` 程式碼已落地（Phase 1 完成）。
> - **Phase 2 未完成**：後端伺服器 `POST /sync/delta` 尚未建立；`syncDelta()` 在無 server 時僅 `console.error`，缺乏「重試 3 次 + 斷點續傳」邏輯。
> - **Phase 3 未完成**：CRDT-lite 多裝置同步為未來規劃，目前僅單裝置。
> - 進度實況：Phase 1（全量同步）✅ 完成；Phase 2（Delta 同步）⏳ 待 Server；Phase 3（CRDT-lite）📅 未排期。

---

### M6：史料完整覆蓋 + 長期增長控管（Full Coverage & Growth Control）

**目標**：全量武將（史實常駐 + 動態後裔可收斂）資料完成 + 增長上限機制

**依賴**：M1-M5 全部

**交付物**：
- 200+ 位武將完整資料（基礎屬性 + 故事條 + 虛擬祖先）
- 戰鬥日誌歸檔策略實作（100 場上限 + 季度摘要）
- 已故武將歸檔機制（Spirit_Card 快照 + 主體移除）
- 動態後裔生命周期策略（史實常駐 / 後裔待刪區 / 英靈卡保留 / 支系摘要壓縮）
- LRU 故事快取（20 位武將常駐上限）
- `DataGrowthMonitor` 服務（定期報告本地儲存使用量）
- 存檔大小健康檢查（warning @ 5MB、error @ 10MB）

**驗收指標**：模擬 1000 天遊玩、500 位武將、10000 場戰鬥 → 存檔 < 5MB compressed

> **⚠️ 長局單機補充備注（2026-04-08）**：
> - `GeneralArchiver` + `SpiritCard` 已具備死亡歸檔基礎，但目前仍偏向「歸檔」而非「真正 GC」。
> - 後續需補 `pending-delete` 隔離層、支系摘要壓縮與 AI 生育配額策略，否則玩家 / AI 長期繁衍仍可能讓存檔無上限膨脹。
> - 資料刪除必須是「可驗證的延後刪除」：先轉 SpiritCard、再保留最小血脈索引、最後才批次清除完整主體。

> **✅ 長局單機生命周期策略實作完成（2026-04-08）**：
> - **Phase 1**（模型層）：`GeneralLifecycle.ts`（A/B/C/D 分類）、`FamilyBranchSummary.ts`、`AIPopulationConfig.ts`、`SpiritCard.ts` 擴充、`PendingDeleteStore.ts`（待刪隔離）
> - **Phase 2**（服務層）：`SeasonalRollup.ts`（季結 hot→cold 壓縮）、`GeneralArchiver.ts` ABCD 路由修改、`DataGrowthMonitor.ts` 降級回調擴充
> - **Phase 3**（管控層）：`BranchCompactor.ts`（已滅絕支系壓縮）、`BreedingQuotaEnforcer.ts` + `IBreedingGate` hook（AI 繁殖配額）
> - **Phase 4**（整合層）：`DataLifecycleScheduler.ts`（統一排程器）、`ai-population-config.json`（人口設定）
> - **任務卡**：DC-6-0007 ～ DC-6-0012（tasks-dc.json 已新增）
> - **LineageSeedStore.ts**（P2）：延後 — 等存檔接近 10MB 時實作
> - **UI spec 任務**：`spirit-card-view` + `branch-summary-view` 待 Phase 1 完成後開立

---

## 七、關鍵技術決策

| 決策 | 選擇 | 理由 |
|------|------|------|
| 本地儲存引擎 | IndexedDB (Web) / SQLite (Native) | JSON 檔超過 1MB 後 parse 耗時劇增；DB 支持分頁查詢 |
| 血統正規化 | Person Registry + 關係圖 | 跨世代引用去重，減少 30% 儲存 |
| 序列化格式 | MessagePack + gzip | 遊戲業界標準，壓縮率 70%+ |
| 同步協議 | JSON Patch (RFC 6902) | 標準化、可驗證、有成熟函式庫 |
| 史料收集 | Wiki 爬取 + AI 生成 | 公開史料自動化，AI 補充故事性內容 |
| 因子儲存 | gene_id 引用 + 共用字典 | 已在用，正確做法 |
| Skills 執行環境 | 本機 Copilot Skills | 零 API 成本，無外部依賴 |
| 稀有度計算 | 雙軸制（maxStat + avg5） | 避免單一指標偏差，取較高 tier 較保守 |

---

## 八、風險與緩解

| 風險 | 影響 | 緩解 |
|------|------|------|
| IndexedDB 在某些瀏覽器達到限額 | 超過 50MB 限制 | 壓縮 + 歸檔策略確保 < 10MB |
| AI 生成故事品質不穩 | 文案風格不一致 | 人工審核 + 風格模板 + 品質閥門 |
| MessagePack 與 Cocos Creator 相容性 | 需要適配 | 用純 JS 版本（不依賴 WASM） |
| 多裝置同步衝突 | 資料遺失 | Phase 3 CRDT-lite，MVP 先單裝置 |
| 動態後裔長局繁衍造成存檔膨脹 | 單機長局存檔失控、載入變慢 | 史實/後裔分區、SpiritCard 快照、pending-delete、支系摘要壓縮、AI 生育配額 |
| Wiki 爬取被封鎖 | 史料收集中斷 | 手動匯入備援；使用 User-Agent 規避限制 |
| 遷移腳本破壞現有資料 | 血統資料損毀 | 遷移前完整備份；空跑 dry-run 確認 |

---

## 九、里程碑時序總覽

```
M1 資料管線基礎
  └─→ M2 分層儲存架構
         └─→ M3 血統正規化
                └─→ M4 壓縮+序列化
                       └─→ M5 增量同步協議
                              └─→ M6 完整覆蓋+增長控管
```

M1 完成後，M2-M6 依序執行。每個里程碑以「驗收指標」為完成門檻。

---

### M7：武將資料編輯工具（General Data Editor Tooling）

**目標**：提供開發期大批量編輯 + 執行期快速驗證雙形態工具，加速武將資料校準與測試閉環

**依賴**：M1（DC-1-0002 validate 工具完成）、M2（DC-2-0002 DataCatalog 完成）

**交付物**：
- `extensions/general-data-editor/` — Cocos Editor Extension（天生不進包）
  - `package.json`：manifest、menu（Open Editor / Validate Data）、IPC messages
  - `src/main.js`：Extension 主體，openPanel / validateData / updateGeneralFields IPC handlers，儲存後自動 rebuild runtime
  - `panels/default/index.js`：HTML table 列表 + 欄位編輯表單
- `assets/scripts/ui/panels/GeneralDataDebugPanel.ts` — Runtime Debug Panel
  - `import { DEV } from 'cc/env'` build-time guard（發布包完全排除）
  - 搜尋 DSL + 即時欄位修改 → DataCatalog.updateEntry

**驗收指標**：
1. Editor Extension：Cocos Editor 啟動後可從 Extension 選單開啟 Panel，搜尋武將，修改一個欄位，儲存後 `validate-generals-data.js` 無 error
2. Runtime Panel：Dev Preview 模式掛載面板，DSL 搜尋可找到武將，修改欄位即時反映到 DataCatalog；Release build 完全不載入此元件

---

## 十、相關文件

| 文件 | 說明 |
|------|------|
| `docs/系統規格書/武將系統.md` | 武將 GeneralConfig 欄位定義 |
| `docs/demo_技術架構.md` | 整體技術架構索引 |
| `docs/程式規格書.md` | 開場流程與資料結構規格 |
| `assets/scripts/core/models/GeneralUnit.ts` | 運行時武將資料模型 |
| `assets/resources/data/generals.json` | 現有武將資料（5 位完整） |
| `assets/resources/ui-spec/content/general-detail-overview-states-v1.json` | UI 預計算 content states |
| `.github/skills/general-data-pipeline/SKILL.md` | 資料管線 Skill |
| `.github/skills/general-balance-tuner/SKILL.md` | 數值平衡 Skill |
| `.github/skills/general-story-writer/SKILL.md` | 故事生成 Skill |
