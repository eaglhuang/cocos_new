<!-- doc_id: doc_spec_0040 -->

> **2026-04-26 覆寫裁定：城市兵源、出征軍勢與召喚式戰鬥**
>
> 1. 正式戰鬥資源鏈為 `人口 -> 城市兵源 -> 出征軍勢 -> 場上分隊`。人口不在戰中逐人扣除，城市兵源才是可見軍事庫存。
> 2. `武將帶兵上限 / Command_Capacity` 改為「本場最多可帶出的兵力數字」，由武將名聲、將階、統率、裝備與事件加成決定，不再是抽象負載條。
> 3. `出征軍勢 / Sortie_Troop_Pool = min(武將帶兵上限, 城市可用兵源, 關卡出征上限)`。戰中每次召喚 / 部署部隊都同時扣 `出征軍勢` 與 `糧草`。
> 4. 每張部隊 / 虎符卡顯示 `投入兵力 Unit_Troop_Cost` 與 `糧草消耗 Food_Cost`。兵力不足、糧草不足、場上部隊數已滿，是唯三主要部署限制。
> 5. 部隊在場上的 `Current_Troops` 即該分隊生命。退場或戰鬥結束時，剩餘兵力返還出征軍勢；被打光的差額列入戰損。
> 6. 戰後結算顯示 `出征軍勢 / 返還兵力 / 本戰損耗 / 城市兵源變化 / 補兵速度 / 預估恢復回合`。`戰備值 / Military_Readiness` 不再作為玩家主顯示資源，只能作為後台摘要或舊欄位相容名。
> 7. 初期戰鬥要保留召喚爽感，正式保底 `Sortie_Troop_Pool_Max` 不可低於 8,000；中期與後期隨官職與治理逐步拉高，不允許把全部兵種壓成過小碎數字。
# 戰場部署系統規格書

> **來源文件**：遊戲系統規格書.md — 第 3 節「5x8 棋盤與戰場平衡 (Battle System)」

---

## A. 系統描述

戰場部署系統定義玩家在 5×8 棋盤戰鬥中的出兵與出征前編制機制。正式版本以**出征軍勢分批投入**與**糧草即時扣除**為核心約束，不再採虎符兵種冷卻。出征前可編制 1 名參謀官與 1 名後勤官：參謀官提供計謀點數上限 / 開局補點，後勤官提供軍糧上限 / 開局補給。每位實際出戰武將仍只可裝 1 張英靈卡與 1 枚虎符；參謀官 / 後勤官屬於 sortie support，不視為額外上場單位。

> **Unity 對照**：DP 系統等同 RTS 中的「供給值（Supply）」——我們改為「出征軍勢 + 糧草即時消耗」；`參謀官 / 後勤官` 更接近隊伍編成中的 `Support Slot` 或 `Formation Buff Slot`；部署失敗提示則類似 RTS 中資源不足時的 disabled button 與 quick suggestion。

---

## B. 系統目的

1. **操作爽度**：無 DP 限制；兵力、糧草或場上部隊不足時，以明確禁用態與改派建議確保玩家知道下一步
2. **戰術深度**：出征軍勢、糧草與場上部隊數共同創造資源壓力與優先序選擇
3. **編制清晰**：把主將、參謀官、後勤官拆成不同責任，避免所有支援能力都硬塞回裝備欄
4. **平衡防火牆**：英靈卡、虎符、參謀官、後勤官各自處理不同資源層，不讓四層效果在最終傷害上失控乘算
5. **自動化體驗**：委任 AI 為非核心戰鬥提供省心選擇（根據武將智力品質導向不同 AI 策略）
6. **經濟連動**：糧草作為戰鬥核心消耗品，連動治理系統的資源產出

---

## C. 商業套路

| 商業點 | 說明 |
|---|---|
| 糧草壓力 | 戰場消耗糧草 → 治理產出不足時驅動補元散/商城購買 |
| 補給包 | 軍糧不足時可用補給包或糧秣禮包補足，屬資源補位而非直接買勝利 |
| 編制便利 | 參謀官 / 後勤官的出征預設欄、快速套用與推薦編制可作為長線便利功能 |
| 降級差距 | 降級普通兵 vs 精銳兵的戰力差距明顯 → 驅動保持糧草充足 |
| 委任便利 | 委任 AI Auto-Battle 可解鎖「自動出征」（月卡附贈/精元購買） |

---

## D. 系統 TA

| 玩家類型 | 使用情境 |
|---|---|
| 操作控 | 手動部署精銳兵種，追求完美糧草與出兵節奏 |
| 策略控 | 計算糧草消耗曲線，決定何時出精銳、何時降級、哪位武將去當參謀官 / 後勤官 |
| 休閒玩家 | 使用委任 AI 自動出征，省心推圖 |
| 課長 | 購買補給便利與編制預設功能，確保高效推關 |
| 三國迷 | 享受主將 / 參謀官 / 後勤官分工與三國軍陣味道 |

---

## E. 製作功能清單

| # | 功能 | 優先級 | 說明 |
|---|---|---|---|
| 1 | 出征軍勢池 | P0 | 本場可投入兵力，來源為 `min(主將帶兵上限, 城市兵源, 關卡上限)` |
| 2 | 糧草即時扣除 | P0 | 每部署一組兵消耗固定糧草，替代 DP 系統 |
| 3 | 場上部隊上限 | P0 | 同場可維持的部隊數上限，避免滿版堆兵 |
| 4 | 部署失敗提示 | P0 | 兵力不足 / 糧草不足 / 場上部隊已滿，三種原因分開提示 |
| 5 | 出征前編制槽 | P0 | 每場戰鬥正式選定 1 名參謀官與 1 名後勤官 |
| 6 | 參謀官計謀點支援 | P1 | 依參謀官 INT / POL 提供計謀點上限與開局補點 |
| 7 | 後勤官軍糧上限支援 | P1 | 依後勤官 LEA / POL 提供軍糧上限與開局補給 |
| 8 | 委任 AI | P1 | 根據武將智力決定自動部署邏輯 |
| 9 | 編制預設 / 推薦 | P2 | 儲存 sortie loadout 與戰前推薦 |

---

## F. 公式相關

### F-0. 兵源鏈與部署合法性

```typescript
// 城市兵源由治理產出；不是總人口，也不在戰中直接扣人口。
City_Troops_Cap =
  floor(Population * Mobilization_Rate * Morale_Mod * Policy_Mod)
  + Barracks_Cap_Bonus;

Recruit_Regen_Per_Turn =
  Base_Recruit_Regen
  + floor(Population * Recruit_Rate * Morale_Mod * Policy_Mod)
  + Military_Task_Output;

Command_Capacity =
  Base_By_General_Rank
  + General.LEA * 50
  + Fame_Bonus
  + Equipment_Command_Bonus
  + Event_Bonus;

Sortie_Troop_Pool_Max = min(
  Command_Capacity,
  City_Troops_Current,
  Stage_Sortie_Cap
);

CanDeploy =
  Sortie_Troop_Pool_Current >= Unit_Troop_Cost
  && Current_Food >= Food_Cost
  && Field_Unit_Count < Field_Unit_Limit;

// 部署時：
Sortie_Troop_Pool_Current -= Unit_Troop_Cost;
Current_Food -= Food_Cost;
SpawnedUnit.Current_Troops = Unit_Troop_Cost;

// 退場 / 結束時：
Returned_Troops = max(0, SpawnedUnit.Current_Troops);
Lost_Troops = Unit_Troop_Cost - Returned_Troops;
Sortie_Troop_Pool_Current += Returned_Troops;

// 戰後：
City_Troops_Current -= Total_Lost_Troops;
Turns_To_Recover = ceil(Total_Lost_Troops / Recruit_Regen_Per_Turn);
```

#### 玩家可見範例

| 狀態 | 數值 |
|---|---|
| 城市人口 | 200,000 |
| 城市兵源 | 10,000 / 10,000 |
| 主將帶兵上限 | 8,000 |
| 本場出征軍勢 | 8,000 |
| 刀盾隊 | 投入 1,000 兵 + 消耗 8 糧 |
| 虎豹騎 | 投入 1,500 兵 + 消耗 18 糧 |

戰中叫出刀盾隊後，HUD 從 `軍勢 8000 / 8000` 變為 `軍勢 7000 / 8000`。若該隊剩 `300 / 1000` 退場，返還 300 兵，本次戰損 700 兵。戰後彙總時，城市兵源從 `10000 / 10000` 變為 `9300 / 10000`，再依治理補兵速度恢復。

### 糧草即時扣除

```typescript
// === 糧草消耗（取代 DP）===
// 一般兵種直接讀基礎表；虎符部隊的正式糧耗仍以兵種（虎符）系統的 TALLY_GRAIN_COST 為準
const BASE_DEPLOY_FOOD_COST: Record<string, number> = {
    INFANTRY:  10,    // 步兵：最便宜
    ARCHER:    12,
    PIKE:      12,
    SHIELD:    14,
    CAVALRY:   18,    // 騎兵：較貴
    NAVY:      20,    // 水軍：需舟船
    SIEGE:     30,    // 機械：最貴
};

function getDeployFoodCost(troopDef: TroopDef, tally: TigerTally | null): number {
    if (troopDef.Is_Special && tally) {
        return TALLY_GRAIN_COST[tally.Quality][tally.Star];
    }
    return BASE_DEPLOY_FOOD_COST[troopDef.Base_Type];
}

// 戰場總糧草預算 = 玩家當前糧草儲備 + 後勤官提供的開局補給
// 戰鬥結束後消耗結算，從玩家資源扣除
```

### 出征前編制槽

```typescript
interface SortieLoadout {
    commander_uid: string;
    strategist_uid: string | null;
    deputy_uid: string | null;
}

const STRATEGIST_TACTIC_CAP_MAX = 30;
const STRATEGIST_TACTIC_START_MAX = 15;
const DEPUTY_FOOD_CAP_RATE_MAX = 0.18;

function getStrategistBonus(strategist: General | null) {
    if (!strategist) return { maxTacticBonus: 0, startTacticBonus: 0 };
    const score = strategist.INT * 0.7 + strategist.POL * 0.5;
    const maxTacticBonus = Math.min(STRATEGIST_TACTIC_CAP_MAX, Math.floor(score / 10));
    const startTacticBonus = Math.min(STRATEGIST_TACTIC_START_MAX, Math.floor(maxTacticBonus * 0.5));
    return { maxTacticBonus, startTacticBonus };
}

function getDeputyFoodBonus(deputy: General | null, baseFoodCap: number) {
    if (!deputy) return { foodCapBonus: 0, startingFoodBonus: 0 };
    const rate = Math.min(DEPUTY_FOOD_CAP_RATE_MAX, 0.03 + deputy.LEA * 0.001 + deputy.POL * 0.0006);
    const foodCapBonus = Math.floor(baseFoodCap * rate);
    return { foodCapBonus, startingFoodBonus: foodCapBonus };
}

function validateSortieLoadout(loadout: SortieLoadout): boolean {
    const ids = [loadout.commander_uid, loadout.strategist_uid, loadout.deputy_uid].filter(Boolean);
    return new Set(ids).size === ids.length;
}
```

### 精銳部署上限與保留槽

```typescript
// === 精銳部署上限 ===
// 只計入「場上存活中的精銳 / 特殊兵 / 攻城器械」；普通小兵不佔上限
const ELITE_DEPLOY_LIMIT = {
    base_active_cap: 4,
    siege_scene_bonus: 1,        // 攻城戰預留 1 個器械槽
    officer_tier_bonus_cap: 1,   // 官職 / 任務加成最多再 +1
};

function getEliteDeployCap(state: BattleState): number {
    const siegeBonus = state.scenePresetId === 'SCENE_SIEGE' ? ELITE_DEPLOY_LIMIT.siege_scene_bonus : 0;
    const officerBonus = Math.min(state.officerTierDeployBonus ?? 0, ELITE_DEPLOY_LIMIT.officer_tier_bonus_cap);
    return ELITE_DEPLOY_LIMIT.base_active_cap + siegeBonus + officerBonus;
}

function canDeployElite(state: BattleState): boolean {
    return state.activeEliteCount < getEliteDeployCap(state);
}
```

- 精銳部署上限的目的，是避免高品質虎符與高糧草存量把戰場直接推成「滿版精銳」。
- `SPECIAL / CAVALRY / NAVY / SIEGE` 預設都算精銳出場名額；`MILITIA` 與基礎步弓盾槍不計入。
- 若超出上限，該卡進入 `場上部隊已滿` 禁用態；系統可提示可派出的低耗部隊，但不得自動把玩家點擊改成其他部隊。

### 部署失敗與改派建議

```typescript
// === 部署檢查 ===
// 兵力、糧草、場上部隊數任何一項不足，目標卡不可部署。
// UI 可以推薦「可派出的低耗部隊」，但玩家必須再次確認，不自動改派。
function deployUnit(player: Player, targetTroop: TroopDef, tally: TigerTally | null, state: BattleState): DeployResult {
    const foodCost = getDeployFoodCost(targetTroop, tally);
    const troopCost = getDeployTroopCost(targetTroop, tally);
    const eliteAllowed = !targetTroop.Is_Special || canDeployElite(state);
    const foodEnough = player.food >= foodCost;
    const troopsEnough = state.sortieTroopsCurrent >= troopCost;

    if (!eliteAllowed) return { unit: null, reason: 'FIELD_UNIT_LIMIT' };
    if (!troopsEnough) return { unit: null, reason: 'TROOPS_SHORT' };
    if (!foodEnough) return { unit: null, reason: 'FOOD_SHORT' };

    player.food -= foodCost;
    state.sortieTroopsCurrent -= troopCost;
    if (targetTroop.Is_Special) state.activeEliteCount += 1;
    return {
        unit: targetTroop.Id,
        quality: targetTroop.Is_Special ? 'ELITE' : 'NORMAL',
        unitTroopCost: troopCost,
        currentTroops: troopCost
    };
    }

const MILITIA_FOOD_COST = 5;   // 普通小兵消耗

// === 普通小兵 vs 精銳 ===
// 普通小兵攻防 = 基礎兵種的 40%
// 沒有特殊能力、沒有兵種剋制加成
// 存在意義：低兵力 / 低糧草時仍有可選低耗部隊，但必須由玩家主動選擇
const MILITIA_STAT_RATIO = 0.4;
```

### 英靈卡 / 虎符 / 支援角色邊界

```typescript
interface BattleLegacyContext {
    spirit_card_id: string | null;
    tally_id: string | null;
    strategist_uid: string | null;
    deputy_uid: string | null;
}

// 1. 英靈卡只提供被動 / 共鳴 / 祖靈語意，不額外召喚第二支部隊
// 2. 虎符才負責特殊兵種調度；同一武將仍只可裝 1 枚虎符
// 3. 參謀官 / 後勤官只提供資源支援，不把自己的虎符或英靈卡效果直接投射到主將身上
```

### 委任 AI

```typescript
// === 委任 AI 邏輯 ===
// 根據武將 INT（智力）決定 AI 部署品質
// 高智力 → 會保留高價值虎符到關鍵時刻，並正確使用參謀官 / 後勤官支援
// 低智力 → 隨機部署、不考慮兵種剋制

type AIDeployQuality = 'LOW' | 'MEDIUM' | 'HIGH' | 'EXPERT';

function getAIDeployQuality(generalINT: number): AIDeployQuality {
    if (generalINT >= 90) return 'EXPERT';   // 諸葛亮級
    if (generalINT >= 70) return 'HIGH';      // 周瑜級
    if (generalINT >= 50) return 'MEDIUM';    // 一般武將
    return 'LOW';                              // 張飛級（勇猛但不擅策略）
}

// AI 部署特性
const AI_DEPLOY_BEHAVIOR: Record<AIDeployQuality, string> = {
    LOW:    '隨機部署，不考慮剋制，精銳立即使用',
    MEDIUM: '基本剋制意識，會保留少量糧草',
    HIGH:   '根據敵方兵種剋制部署，保留高價值虎符到關鍵時刻',
    EXPERT: '完美剋制 + 正確保留虎符與補給窗口，糧草與計謀管理最優',
};

const AI_RESERVE_POLICY = {
    LOW:    { keepFoodFloorRate: 0.00, reserveSpecialForBoss: false, reserveSiegeSlots: 0 },
    MEDIUM: { keepFoodFloorRate: 0.10, reserveSpecialForBoss: false, reserveSiegeSlots: 0 },
    HIGH:   { keepFoodFloorRate: 0.18, reserveSpecialForBoss: true,  reserveSiegeSlots: 1 },
    EXPERT: { keepFoodFloorRate: 0.25, reserveSpecialForBoss: true,  reserveSiegeSlots: 1 },
};
```

| AI 品質 | 保留策略 | 正式定義 |
|---|---|---|
| LOW | 幾乎不保留資源 | 看到可用精銳就直接下，不區分主將戰或場景目標 |
| MEDIUM | 保留少量糧草 | 會避免在前兩回合把糧草耗空，但仍可能提早交掉虎符兵 |
| HIGH | 保留 1 個特殊兵 / 器械槽 | 會等到敵方主將、營帳、城門等關鍵目標出現再下高價值單位，並盡量保住參謀官 / 後勤官提供的節奏優勢 |
| EXPERT | 嚴格保留特殊兵與器械節奏 | 會依 `scenePresetId` 與敵方陣容，把高 `TigerTallyScore` 單位留給決勝窗口，並正確利用計謀點 / 補給上限 |

---

## G. 劇本相關

| 情境 | 劇本需求 |
|---|---|
| 兵力不足 | 「軍勢不足，無法派出 {TroopName}。」 |
| 糧草不足 | 「糧草不足，無法派出 {TroopName}。」 |
| 場上部隊已滿 | 「場上部隊已滿，請先等待退場或撤回部隊。」 |
| 改派建議 | 「可改派低耗部隊：{TroopName}。」 |
| 參謀官支援 | 「參謀官 {GeneralName} 籌策完畢，計謀點上限 +{Value}！」 |
| 後勤官支援 | 「後勤官 {GeneralName} 押運補給，軍糧上限 +{Value}！」 |
| 委任 AI | 「{GeneralName} 接管指揮權——委任自動出兵。」 |
| 糧草耗盡 | 「糧草耗盡……無法再出兵！」 |

---

## H. 字串內容相關

| Key | 預設值 | 備註 |
|---|---|---|
| UI_FOOD_COST | 「糧草 -{Cost}」 | 部署消耗提示 |
| UI_TROOP_COST | 「軍勢 -{Cost}」 | 部署兵力提示 |
| UI_TROOPS_SHORT | 「⚠ 軍勢不足」 | 部署禁用提示 |
| UI_FIELD_LIMIT_FULL | 「⚠ 場上部隊已滿」 | 部署禁用提示 |
| UI_DEPLOY_SUGGESTION | 「可改派：{TroopName}」 | 改派建議 |
| UI_STRATEGIST_SUPPORT | 「參謀官 {GeneralName}：計謀上限 +{Value}」 | |
| UI_DEPUTY_SUPPORT | 「後勤官 {GeneralName}：軍糧上限 +{Value}」 | |
| UI_DELEGATE_AI | 「委任 {GeneralName} 自動指揮」 | |
| UI_FOOD_EMPTY | 「⚠ 糧草耗盡！」 | |
| UI_SUPPORT_ROLE_LOCK | 「同一武將不可同時擔任兩個出征角色」 | |

---

## I. Data Schema 需求

### 本機端 (Client)

```json
{
  "Sortie_Loadout": {
    "Commander_UID": "G501",
    "Strategist_UID": "G188",
    "Deputy_UID": "G233",
    "Strategist_Max_Tactic_Bonus": 18,
    "Strategist_Start_Tactic_Bonus": 9,
    "Deputy_Food_Cap_Bonus": 1200
  },
  "Battle_Deploy_State": {
    "Current_Food": 9700,
    "Max_Food": 13200,
    "Current_Tactic_Points": 21,
    "Max_Tactic_Points": 48,
    "Sortie_Troop_Pool_Max": 8000,
    "Sortie_Troop_Pool_Current": 7000,
    "Field_Unit_Limit": 4,
    "Field_Unit_Count": 2,
    "Elite_Deploy_Cap": 4,
    "Active_Elite_Count": 2,
    "Scene_Preset_ID": "SCENE_NIGHT_RAID",
    "Reserved_Tally_Bands": ["SSR", "UR"],
    "Strategist_UID": "G188",
    "Deputy_UID": "G233",
    "Delegate_AI_Active": false,
    "Militia_Deployed": 2,
    "Elite_Deployed": 5
  }
}
```

### Server 端

| 欄位 | 型別 | 說明 |
|---|---|---|
| player_id | VARCHAR(32) FK | 玩家 |
| battle_id | VARCHAR(32) PK | 戰鬥 ID |
| commander_uid | VARCHAR(32) FK | 本場主將 |
| strategist_uid | VARCHAR(32) FK NULL | 本場參謀官 |
| deputy_uid | VARCHAR(32) FK NULL | 本場後勤官 |
| food_spent | INT | 本場消耗糧草 |
| sortie_troops_max | INT | 本場出征軍勢上限 |
| troops_lost | INT | 本場戰損兵力 |
| troops_returned | INT | 本場返還兵力 |
| food_cap_bonus | INT | 後勤官帶來的軍糧上限加成 |
| tactic_cap_bonus | INT | 參謀官帶來的計謀點上限加成 |
| tactic_start_bonus | INT | 參謀官帶來的開局計謀點加成 |
| elite_cap | TINYINT | 本場精銳部署上限 |
| militia_count | INT | 普通小兵部署次數 |
| elite_count | INT | 精銳部署次數 |
| scene_preset_id | VARCHAR(32) | 套用的場景 / Tile preset |
| delegate_used | BOOLEAN | 是否使用委任 AI |

---

## J. 名詞定義

| 名詞 | 代碼/Key | 定義 |
|---|---|---|
| 糧草即時扣除 | Food_Deploy_Cost | 每部署一組兵扣除對應糧草，取代傳統 DP 系統 |
| 出征軍勢 | Sortie_Troop_Pool | 本場可分批投入的兵力池 |
| 投入兵力 | Unit_Troop_Cost | 部署一支部隊時扣除的出征軍勢 |
| 返還兵力 | Returned_Troops | 部隊退場或戰鬥結束時回到軍勢池的剩餘兵力 |
| 戰損兵力 | Lost_Troops | 投入兵力扣除返還兵力後的損耗，戰後回扣城市兵源 |
| 出征編制 | Sortie_Loadout | 戰前選定主將 / 參謀官 / 後勤官的正式配置 |
| 參謀官支援 | Strategist_Support | 由參謀官提供的計謀點上限與開局補點加成 |
| 後勤官補給 | Deputy_Supply_Bonus | 由後勤官提供的軍糧上限與開局補給加成 |
| 改派建議 | Deploy_Suggestion | 原目標不可部署時，UI 推薦玩家可主動改派的低耗部隊 |
| 普通小兵 | Militia | 低耗基礎單位，攻防為精銳的 40%，無特殊能力 |
| 委任 AI | Delegate_AI | 根據武將智力品質自動決定部署策略的自動戰鬥系統 |
| 精銳部署上限 | Elite_Deploy_Cap | 戰場同時可維持的精銳 / 特殊兵 / 器械數量上限 |
| 保留槽 | Reserve_Slot | AI 或規則預留給高價值虎符兵、攻城器械的部署容量 |
| 場景預設 | Scene_Preset_ID | 場景戰法注入到戰場時所套用的 Tile-State preset 識別碼 |
| 角色鎖 | Command_Role_Lock | 同一武將不得在同一場 battle 同時擔任主將、參謀官或後勤官兩個以上角色 |

---

## K. 特殊戰場環境與模組化格子補充

> **整併來源**：戰場互動矩陣與養成動力.md (doc_spec_0091)、夜襲戰場視覺規格提案.md (doc_spec_0078)、模組化戰場系統開發策略.md (doc_spec_0089)
> **衝突處理**：本章是在現行 5×8 棋盤、糧草與精銳部署上限體系之上疊加環境變體，不改動主戰場的基本回合框架。
> **完整規格**：場景戰法（Scene Gambit）與格子戰法（Tile Tactic）的完整定義、觸發流程、交互規則，請見 **戰法場景規格書.md (doc_spec_0039)**。

### 戰場環境的正式定位

- 戰場特殊玩法不應靠另做一張完全獨立的棋盤系統，而應透過「格子狀態 + 任務變數 + 視覺主題」在同一個 5×8 基礎上切換。
- **場景戰法**（Scene Gambit）由高 INT 參謀官在戰場外發動成功後，進入戰鬥時載入預繪靜態背景 + 預設 Tile-State 佈局。場景背景是畫死的，不可動態切換。
- **格子戰法**（Tile Tactic）是武將在戰場上消耗 TP 施放的即時技能，僅在指定格子上疊加特效（如火攻幾格、落石幾格、暴風幾格），不改變背景。
- 夜襲、火攻、圍堵等只是場景變體，不可覆蓋主規則中的出征軍勢、糧草、場上部隊上限、改派建議與 sortie support 邏輯。

### 已規劃的 6 種場景戰法

| 場景 ID | 場景名稱 | INT 門檻 | 預繪背景風格 | 核心玩法 |
|---|---|---|---|---|
| SCENE_FOREST_AMBUSH | 森林埋伏戰 | ≥ 75 | 冷色夜戰密林 | 陷阱抓捕、隱身伏擊 |
| SCENE_NIGHT_RAID | 夜襲作戰 | ≥ 70 | 月光營帳群 | 佔領營帳、夜間暴擊 |
| SCENE_CANYON_ROCK | 峽谷落石戰 | ≥ 65 | 險峻峽谷 | 落石傷害、路線封鎖 |
| SCENE_FLOOD | 水淹七軍戰 | ≥ 80 | 深藍水體渡河 | 水流推移、適性削弱 |
| SCENE_FIRE_RAID | 火燒偷襲戰 | ≥ 75 | 暗暖橘色火場 | 火焰蔓延、物資掠奪 |
| SCENE_SIEGE | 攻城戰 | ≥ 60 | 城牆護城河 | 器械攻城、城牆地利 |

### Tile-State 模組分類

| 模組 | 代表功能 | 典型戰場 |
|---|---|---|
| Normal Tile | 一般移動與接戰 | 標準遭遇戰 |
| Hazard Tile | 進入或停留時受傷害 | 火燒 / 落石 |
| Force-Move Tile | 強制推移、退後、滑移 | 水淹 / 潮汐 |
| Stealth Tile | 隱匿、伏擊、陷阱 | 森林埋伏 / 夜襲死角 |

### Tile-State preset 權威表

| Preset_ID | 對應場景 | 組成重點 | 對部署的直接影響 |
|---|---|---|---|
| `SCENE_FOREST_AMBUSH` | 森林埋伏戰 | 高比例 `STEALTH` + 陷阱 `HAZARD` | 優先保留機動與伏擊單位，低視野不鼓勵早交遠程精銳 |
| `SCENE_NIGHT_RAID` | 夜襲作戰 | `STEALTH`、營帳目標、局部火源 `HAZARD` | AI 應保留高爆發虎符兵給營帳與主將擊殺窗口 |
| `SCENE_CANYON_ROCK` | 峽谷落石戰 | 固定落石 `HAZARD` + 路線封鎖 | 精銳部署以耐打前排與擊退單位優先 |
| `SCENE_FLOOD` | 水淹七軍戰 | 大量 `FORCE_MOVE` + 深水 `HAZARD` | 優先水戰 / 位移抗性單位，減少低適性騎兵投入 |
| `SCENE_FIRE_RAID` | 火燒偷襲戰 | 可蔓延 `HAZARD` + 局部 `STEALTH` | 高火抗與高機動單位優先，避免把低韌性精銳一次壓上 |
| `SCENE_SIEGE` | 攻城戰 | 城牆阻隔、護城河 `HAZARD`、器械通道 | 啟用器械保留槽，保證攻城器械不被一般精銳擠掉 |

- 模組化棋盤的正式原則是「**同一張 5×8 棋盤 + 不同 preset**」，而不是為每種場景再做一套新棋盤系統。
- `scenePresetId` 只控制格子狀態、部署權重與目標物件，不改動本系統的出征軍勢、糧草、場上部隊上限、改派建議與 sortie support 主規則。

### 養成與戰場互動矩陣

- 高機動角色更適合切後排、繞背與利用 Force-Move 地圖。
- 高力量角色更適合擊退、破陣與把敵方推入 Hazard Tile。
- **高智力角色**更適合觸發場景戰法（Scene Gambit），放大場外軍事任務的範圍與成功率。
- 高意志 / 韌性角色更適合抵抗地圖型 Debuff 與特殊環境懲罰。
- 高運勢角色更適合承接掠奪、寶物與事件型戰利品掉落。


---
## 🗳 MCQ 決策記錄（Q33）

- **問題**：格子屬性 JSON 結構
- **衝突說明**：戰場格子屬性表尚未定型資料結構，靜態JSON與ScriptableObject各有優缺，不拍板會導致戰場系統各部分各自選型無法橫向擴充。
- **裁決**：**選項 D** — 選項 D
- **回寫時間**：2026-04-12 13:16
- **來源**：由 `consolidation-doubt-mcq.js rewrite-all` 自動寫入

---


---
## 🗳 MCQ 決策記錄（Q61）

- **問題**：虎符特種兵部署冷卻回合機制
- **衝突說明**：doc_spec_0109（虎符系統設計與商業化探討）明確提出：英靈精銳每10回合才能出一波，普通精銳每3回合可出；召喚消耗糧草，此冷卻是核心平衡閥。doc_spec_0107（虎符系統平衡與養成策略）也強調英靈虎符冷卻極長、轉蛋精銳虎符冷卻短，以此區隔兩類定位。但現行 doc_spec_0012（兵種虎符系統）完全沒有特種兵部署冷卻機制，只有星級/數值系統。若不加冷卻，英靈部隊可能被連續召喚，導致平衡崩盤；若加冷卻，需新增 TigerTally.Cooldown_Turns 欄位並更新 BattleController 邏輯。
- **裁決**：**選項 C** — 維持現有星級+數值系統，不設特種兵部署冷卻
- **回寫時間**：2026-04-13 00:34
- **來源**：由 `consolidation-doubt-mcq.js rewrite-all` 自動寫入

---


---
## 🗳 MCQ 決策記錄（Q62）

- **問題**：特種軍隊召喚是否消耗糧草
- **衝突說明**：doc_spec_0109（虎符系統設計與商業化探討）提出：召喚英靈部隊瞬間扣除大量糧草，強迫玩家在「維持戰線（普通兵）」與「瞬間爆發（英靈兵）」之間取捨，並且可課金購買「點將令」刷新冷卻。doc_spec_0107 也暗示「轉蛋精銳糧草消耗中等、英靈虎符糧草消耗高」是差異化手段之一。但現行 doc_spec_0012 完全沒有糧草消耗機制，也沒有「點將令」道具定義。若採納，需在 TigerTally schema 新增 GrainCost 欄位，並在 BattleController 加入召喚扣糧邏輯。
- **裁決**：**選項 A** — 加入糧草消耗（英靈精銳召喚扣大量糧草，普通精銳扣中等糧草）
- **回寫時間**：2026-04-13 00:34
- **來源**：由 `consolidation-doubt-mcq.js rewrite-all` 自動寫入

---


---
## 🗳 MCQ 決策記錄（Q63）

- **問題**：英靈虎符vs精銳虎符商業差異化是否正式化
- **衝突說明**：doc_spec_0107 與 doc_spec_0109 均建議將虎符分為「英靈虎符（Heritage Seal）= 名將死亡轉化，有招牌技能但冷卻長/糧耗高」與「精銳虎符（Gacha Seal）= 常態數值穩定，低冷卻低糧耗」。此二元差異化是兩份文件的核心商業主張。但現行 doc_spec_0012 只有「死亡傳承虎符」和「名將專屬虎符」兩張表，兩者都走相同的星級/品質公式，沒有「英靈主動技能爆發 vs 精銳持續輸出」的明確規格。若採 A，需在 TigerTally 新增 TallyCategory（SPIRIT/ELITE）欄位並配合冷卻/糧耗欄位；若採 B，維持現有 schema 不動，但失去討論文件的核心商業亮點。
- **裁決**：**選項 B** — 維持現有統一品質體系（R/SR/SSR/UR），不做英靈/精銳概念二分
- **回寫時間**：2026-04-13 00:34
- **來源**：由 `consolidation-doubt-mcq.js rewrite-all` 自動寫入

---


---
## 🗳 MCQ 決策記錄（Q64）

- **問題**：同陣容英靈虎符集齊羈絆加成是否正式化
- **衝突說明**：doc_spec_0107 明確提出：若同時裝備多個同陣營英靈，冷卻時間集體縮短 10%，以刺激玩家去收集完整名將陣容。doc_spec_0109 也提到「集齊五虎將或江東十二虎臣的英靈虎符」作為核心收集動力。現行 doc_spec_0012 在 F 節已定義家族共鳴（+8%）與史實羈絆（+5%），但沒有「集齊特定組合觸發 Set buff」的概念。若採 A，需要新增 SetBonusDef 資料表並在 BattleController 判定玩家當前陣容是否滿足 Set 條件；若採 B 則維持現有 schema 不變。
- **裁決**：**選項 A** — 加入 Set 羈絆機制（集齊特定組合如五虎將英靈符 → 觸發同場攻防加成）
- **回寫時間**：2026-04-13 00:34
- **來源**：由 `consolidation-doubt-mcq.js rewrite-all` 自動寫入

---


---
## 🗳 MCQ 決策記錄（Q65）

- **問題**：委任AI是否根據智力自動判斷英靈虎符使用時機
- **衝突說明**：doc_spec_0109（虎符系統設計與商業化探討）提出：若武將智力夠高，他會自動幫玩家「存」英靈虎符，直到遇到敵方名將才放出來。這是對應武將 INT（智力）屬性實現的委任 AI 行為。現行 doc_spec_0012 完全沒有定義委任 AI 在虎符使用上的決策邏輯；同時 doc_spec_0040（戰鬥部署）也只提到「精銳上限 + AI 保留槽」但未涉及英靈虎符自動觸發。若採 A，需在 AI 武將邏輯中加入 TallyDecisionPolicy，評估是否是觸發英靈部隊的好時機；若採 B，則虎符觸發純靠玩家操作。
- **裁決**：**選項 D** — 選項 D
- **回寫時間**：2026-04-13 00:34
- **來源**：由 `consolidation-doubt-mcq.js rewrite-all` 自動寫入

---


## 格子移動與戰場格子系統連動
- **移動範圍**：依據武將的機動力（速度），每回合移動範圍為 3~5 格。高機動可繞後攻擊脆弱參謀官。
- 戰場中的環境限制由 戰場格子系統.md 定義（如：落石阻擋、泥濘水流）。
