# 武將人物介面規格書

> **文件目的**：定義武將人物介面的正式資訊架構、顯示規則、鎖定規則與資料對映，作為 `GeneralDetailPanel`、大廳武將卡、詳情彈窗、血統/戰法/適性延伸畫面的母規格。
> **適用範圍**：武將角色頁、詳情頁、彈窗頁、人物卡、收藏頁、未來培育頁的角色摘要模組。
> **核心原則**：預設顯示已知資訊；未解鎖、未發現或不想揭露的欄位，一律以鎖圖示 `🔒`、`?` 佔位、折疊區塊或灰階卡表現。
> **Unity 對照**：此介面相當於 Unity 的角色詳情面板 + 樣板卡片 + 多頁籤資訊視圖，由 ScriptableObject 資料驅動，View 只負責呈現。

---

## 1. 文件定位與設計目標

### 1.1 定位

武將人物介面是玩家查看「一名武將完整面貌」的主要入口。它不是單純的數值頁，而是結合以下資訊的正式角色介面：

1. 基本身份與生命週期
2. 六色屬性與能力評語
3. 血統、因子、爆發力與覺醒狀態
4. 戰法、教官啟發與習得狀態
5. 兵種、地形、天氣適性
6. 隱藏欄位、未解鎖欄位與折疊欄位

### 1.2 設計目標

1. 讓玩家一眼辨識這名武將「是誰、強在哪、能做什麼、還缺什麼」
2. 兼容正式版的長線養成資料，避免未來擴欄位時大改 UI
3. 保留手機可讀性，避免單頁資訊過載
4. 對接現有 `GeneralUnit` 與 `GeneralDetailPanel`，減少實作轉換成本
5. 讓血統、戰法、適性這三大子系統在同一頁形成完整敘事

### 1.3 非目標

1. 不在這份文件中定義戰鬥數值公式本體
2. 不在這份文件中定義培育流程本體
3. 不在這份文件中定義轉蛋或抽卡 UI
4. 不把所有隱藏資訊直接揭露給玩家

---

## 2. 依據來源

本規格主要對齊以下正式規格：

1. [武將系統.md](武將系統.md)
2. [血統理論系統.md](血統理論系統.md)
3. [戰法系統.md](戰法系統.md)
4. [戰場適性系統.md](戰場適性系統.md)
5. [demo_playbook.md](../demo_playbook.md)
6. [demo_技術架構.md](../demo_技術架構.md)
7. [UI技術規格書.md](../UI技術規格書.md)
8. [GeneralUnit.ts](../../assets/scripts/core/models/GeneralUnit.ts)
9. [GeneralDetailPanel.ts](../../assets/scripts/ui/components/GeneralDetailPanel.ts)

---

## 3. 介面總覽

### 3.1 建議容器形式

武將人物介面建議採用「頁籤式詳情彈窗」或「抽屜式人物面板」。

#### 方案 A：頁籤式詳情彈窗
- 適合完整展示多系統資訊
- 適合中後期正式版
- 建議放在 PopupLayer

#### 方案 B：人物卡 + 詳情抽屜
- 適合大廳、列表、收藏頁
- 點擊人物卡後右側或底部滑出詳情
- 適合手機橫豎切換

### 3.2 建議頁面結構

```text
GeneralDetailPanel
├─ Header（頭像、名稱、稱號、陣營、職能、狀態）
├─ Summary Strip（HP、SP、年齡、精力、EP 評級）
├─ Tab Bar
│  ├─ 基本資料
│  ├─ 六色屬性
│  ├─ 血統與因子
│  ├─ 戰法與技能
│  ├─ 戰場適性
│  └─ 延伸資訊
└─ Footer Actions（返回、收藏、鎖定、比較、分享）
```

### 3.3 顯示優先級

1. 必顯：名稱、稱號、陣營、職能、狀態、HP、SP、年齡、精力
2. 高優先：六色屬性、EP 評級、血統簡覽、已習得戰法、兵種/地形適性
3. 中優先：因子槽、血脈警告、可習得戰法、戰法等級、天氣適性
4. 低優先：UID、Bloodline_ID、Template_ID、來源標籤、進階劇本標籤

### 3.3.1 血脈內容優先級補充

- 武將立繪不再只是展示美術，而是進入 `表象面 / 血脈面` 的切換入口
- 角色內容層正式分成兩種：
  - `歷史趣聞`：對應史實逸事、人格魅力與大眾熟悉記憶
  - `血脈傳聞`：對應命紋靈獸、祖紋命篆與其勇猛、謀略、魅力來源的合理化敘事
- 兩種故事內容在視覺上應共用同一個抽屜層，但標籤、色調與標題要能一眼區分

### 3.4 框體分層視覺規格

武將人物介面若要達到正式水墨渲染質感，不應再把每個 panel 視為一張單獨的九宮格框，而應拆成可疊合的框體家族。

Unity 對照：這相當於把一個 Panel Prefab 拆成 `Frame`、`Inner Glow/Bleed`、`Fill` 三個子 Image，再由外層容器統一排版；而不是只放一張 sliced sprite 撐完整個視覺。

#### 分層原則

1. `frame`：只承擔外輪廓、金屬紋飾、角花與主要結構線，避免把大面積灰霧直接烘在最外框。
2. `bleed`：負責邊框往內容區滲入的墨暈、霧邊與暗角，是讓框與背景自然過渡的主因。
3. `fill`：負責內容區的大面積紙感、煙墨、山霧或深色底紋，不應以單色實心底板取代。
4. `accent`：只用於 active tab、稀有度高亮、警示狀態或頁面焦點，不應變成每個 panel 都有的常駐發光層。

#### 套用範圍

1. `PopupBody / RightContentArea` 使用大型框體家族，必須具備 `frame + bleed + fill` 三層。
2. `HeaderPanel`、`SummaryStrip`、`FooterPanel` 使用中型框體家族，至少具備 `frame + fill`，建議補 `bleed`。
3. 頁內卡片如血統摘要、技能卡、適性卡使用 section 家族，不得再直接與 header 共用完全相同的視覺層級。
4. `RightTabBar` 與 tab button 應拆成「tab rail」與「tab plate」兩種家族，避免整條頁籤列和單顆按鈕共享同一張小卡框。

#### 質感目標

1. 讓邊框與背景的過渡發生在 `bleed` 層，而不是靠透明邊緣硬撐。
2. 讓角色立繪後方的大底圖、右側內容框、頁內卡片維持同色系不同密度，而不是各自獨立成一塊塊貼紙。
3. 讓 active 與 idle 的差異主要來自 `accent` 與亮度階層，不是單純換色塊。

### 3.5 血脈世界觀微調原則

- 本頁面不做整體 UI 翻新，只在既有骨架上做世界觀微調
- 允許新增的元素：
  - `祖紋命篆` 紋樣
  - `命紋靈獸` 小型 badge / 虛影 / 角標
  - 暗金、玉白、月銀、墨青、暗赤的微調色階
  - 血脈面點亮時的墨痕、玉刻光痕、祖紋連線
- 不允許新增的方向：
  - 科技感 HUD 線條
  - 恐怖污染或怪物化裂臉
  - 重奇幻神魔粒子鋪滿畫面
- 角色立繪的改動原則是 `符號加法`，不是推翻原本商品化畫風

### 3.6 日常人物頁 v2 母型

依據目前概念 proof，日常人物頁正式收斂為 `人物主導 + 血脈輔助 + 故事條固定化` 的骨架。

#### 桌面版首選結構

1. 左側 `38%~42%`
   - 近全身角色主立繪
   - 人物永遠是第一主角
2. 中右主資訊區 `42%~48%`
   - 名稱、稱號、稀有度、五屬、戰鬥特性
   - 使用單一主卡或同語系分層卡片，不做破碎小模組拼貼
3. 右側窄欄 `8%~12%`（可選）
   - 頁籤 rail、切換器或血統標籤
4. 下方固定故事條 `16%~22%` 高
   - 5 格或 6 格連續故事圖
   - 作為頁面的一部分，而不是小型附贈漫畫

#### 血脈區定位

- 日常人物頁中的血脈區不是第二主角，也不是雙面世界
- 它應像 `深層人格區`：
  - 血脈摘要
  - 覺醒傾向
  - 血脈性格註記
  - 命紋靈獸徽記
- 不應做成另一張大卡把人物重複一遍

#### 故事條定位

- 桌面版 premium layout 優先使用固定故事條，而不是預設抽屜
- 故事條建議 5~6 格，由左往右讀
- 節奏建議：
- 前段：歷史人生
- 中段：命運轉折
- 後段：血脈暗示 / 祖靈牽引

### 3.7 日常人物頁 v3 與多分頁整合原則

- `GeneralDetailPanel` 仍是本規格的正式功能容器，負責完整的多分頁人物詳情。
- `GeneralDetailBloodlineV3` 的定位不是另一套平行正式人物頁，而是 `GeneralDetail` 的預設首頁 / 總覽態視覺骨架。
- 玩家從大廳武將列表點入人物頁時，第一眼應優先看到 `日常人物頁 v3` 的總覽畫面，再由頁籤進入 `基本資料 / 六維屬性 / 血統與因子 / 戰法與技能 / 戰場適性 / 延伸資訊`。
- `日常人物頁 v3` 承接的是 `人物立繪 + 核心摘要 + 血脈摘要 + 5~6 格故事條`，屬於高可讀的首頁殼層；它不是用來取代後續深度分頁。
- 實作上允許把 `GeneralDetailBloodlineV3` 做成第一個頁籤、預設展開態，或 `Overview` 狀態，但不得把既有詳情分頁功能直接刪除或降格成次要規格。
- 欄位分工上，`日常人物頁 v3` 應優先承接 `TabBasics` 的核心身分資訊與 `TabBloodline` 的摘要資訊；完整血統矩陣、因子細節、戰法細項與適性列表仍保留在對應分頁。

---

## 4. 資料來源對齊

### 4.1 與 `GeneralUnit` 的直接對應

| GeneralUnit 欄位 | 介面顯示位置 | 顯示方式 |
|---|---|---|
| `id` | 延伸資訊 | 低優先文字，可複製 |
| `name` | Header | 主名稱 |
| `faction` | Header / 基本資料 | 陣營標籤 |
| `maxHp` / `currentHp` | Summary Strip | HP 條與數值 |
| `maxSp` / `currentSp` | Summary Strip | SP 條與數值 |
| `str` | 六色屬性 | 屬性數值 |
| `int` | 六色屬性 | 屬性數值 |
| `lea` | 六色屬性 | 屬性數值 |
| `luk` | 六色屬性 | 屬性數值 |
| `skillId` | 戰法與技能 | 主技能識別 |
| `attackBonus` | 基本資料 / 戰法 | 攻擊加成提示 |
| `preferredTerrain` | 戰場適性 | 擅長地形 |
| `terrainDefenseBonus` | 戰場適性 | 地形防禦加成 |

### 4.2 與武將系統的補充欄位

| 欄位 | 來源系統 | 介面用途 |
|---|---|---|
| `Template_ID` | 武將系統 | 同名版本辨識 |
| `Title` | 武將系統 | 稱號標籤 |
| `Gender` | 武將系統 | 僅在詳細頁顯示 |
| `Age` | 武將系統 | 生命週期提示 |
| `Vitality` | 武將系統 | 精力條與狀態 |
| `Role` | 武將系統 | Combat / Mentor 標籤 |
| `Lifespan` | 武將系統 | 隱藏天命值，可遮蔽 |
| `Status` | 武將系統 | Active / Training / Retired / Dead |

### 4.3 與血統理論系統的補充欄位

| 欄位 | 來源系統 | 介面用途 |
|---|---|---|
| `Ancestors_JSON` | 血統理論系統 | 14 人血統矩陣 |
| `Bloodline_ID` | 血統理論系統 | 近親判定與警告 |
| `EP` | 血統理論系統 | 爆發力評級 |
| `EP_Rating` | 血統理論系統 | 天命分級文字 |
| `Genes[]` | 血統理論系統 | 因子槽卡片 |
| `Awakening_Title` | 血統理論系統 | 覺醒名號 |
| `Virtual_Ancestor` | 血統理論系統 | 虛擬祖先標記 |

### 4.4 與戰法系統的補充欄位

| 欄位 | 來源系統 | 介面用途 |
|---|---|---|
| `Learned_Tactics[]` | 戰法系統 | 已習得戰法列表 |
| `Inspired_Tactics[]` | 戰法系統 | 教官啟發後可購買清單 |
| `TacticLevel` | 戰法系統 | 戰法等級 |
| `TP_Cost` | 戰法系統 | 戰法消耗提示 |
| `TacticType` | 戰法系統 | 分類標籤 |

### 4.5 與戰場適性系統的補充欄位

| 欄位 | 來源系統 | 介面用途 |
|---|---|---|
| `Troop_Aptitude` | 戰場適性系統 | 兵種適性矩陣 |
| `Terrain_Aptitude` | 戰場適性系統 | 地形適性矩陣 |
| `Weather_Aptitude` | 戰場適性系統 | 天氣適性矩陣 |
| `Aptitude_Level` | 戰場適性系統 | S~D 星級 |

---

## 5. UI 資訊架構

### 5.1 Header 區

Header 是人物介面的第一視覺焦點，必須穩定呈現以下內容：

1. 頭像 / 立繪 / 角色剪影
2. 名稱
3. 稱號
4. 陣營
5. 職能（Combat / Mentor）
6. 狀態（Active / Training / Retired / Dead）
7. 稀有度 / 版本標籤（若有）
8. 命紋靈獸 badge 或祖紋命篆縮記號
9. 立繪點擊提示（可進入血脈面 / 故事層）

### 5.2 Summary Strip 區

Summary Strip 放在 Header 下方，快速顯示核心狀態：

1. HP
2. SP
3. 年齡
4. 精力
5. EP 分級
6. 主技能是否可用

### 5.3 Tab 1：基本資料

基本資料頁顯示不需要展開即可理解的核心資訊。

#### 需顯示欄位
- UID
- Template_ID
- Name
- Title
- Faction
- Gender
- Age
- Role
- Status
- Lifespan
- Vitality_Current / Vitality_Max

#### 建議視覺
- 左側放頭像與狀態章
- 右側放文字欄位與短標籤
- `Lifespan` 建議預設隱藏為「天命值：🔒」或條件顯示

### 5.4 Tab 2：六色屬性

六色屬性頁用於顯示能力骨架與戰鬥定位。

#### 需顯示欄位
- STR
- INT
- LEA
- POL
- CHA
- LUK

#### 視覺規則
- 採列表 + 雷達圖雙呈現
- 主力屬性高亮
- 未主修屬性灰化但不隱藏
- 可附帶一句定位評語，例如「武力型」、「謀略型」、「統率型」

### 5.5 Tab 3：血統與因子

這是本規格最重要的區塊之一，需兼顧輕度與硬核玩家。

#### 預設內容
1. EP 卡片
2. EP_Rating 文案
3. 父母簡覽
4. 因子槽簡圖
5. 近親警告或歸宗標籤
6. 命紋靈獸名稱 / 縮圖
7. 血脈面狀態提示
8. `歷史趣聞 / 血脈傳聞` 入口

#### 展開內容
1. 14 人血統矩陣
2. 虛擬祖先標記
3. Bloodline_ID
4. 覺醒名號
5. 因子來源軌跡
6. 祖紋命篆說明
7. 命紋靈獸圖騰與對應符號 slot
8. 父母系譜與因子流向
9. 子嗣預測摘要與覺醒預兆

### 5.6 Tab 4：戰法與技能

#### 需顯示欄位
- 主戰法
- 已習得戰法列表
- 可習得戰法列表
- 未啟發戰法列表
- 戰法等級
- TP 消耗
- 戰法類型
- 技能簡述

#### 三態規則
1. 已習得：正常彩色顯示，可點擊看詳情
2. 可習得但未啟發：半透明、鎖定、顯示教官需求
3. 無法習得：灰階、鎖定、顯示因子不符或條件未滿

### 5.7 Tab 5：戰場適性

#### 需顯示欄位
- 兵種適性：騎 / 步 / 弓 / 機械
- 地形適性：平原 / 山地 / 水域 / 林地 / 沙漠
- 天氣適性：晴 / 雨 / 霧 / 風 / 夜 / 雷

#### 視覺規則
- 採 S / A / B / C / D 星級或級別卡
- S 級顯示金色高亮
- D 級顯示警示色與排斥符號
- 可展開文字說明對應加成

### 5.8 Tab 6：延伸資訊

#### 需顯示欄位
- UID
- Template_ID
- Bloodline_ID
- 來源標籤
- 取得方式
- 成長摘要
- 戰績摘要
- 收藏狀態
- 英靈虎符摘要
- 血脈卡等級 / 血脈濃度
- 特種軍隊連動標籤
- 分享 / 比較 / 鎖定按鈕

---

## 6. 鎖定與遮蔽規則

### 6.1 鎖圖示規則

以下欄位預設使用鎖圖示 `🔒` 或 `?` 佔位：

1. 未解鎖因子
2. 未啟發戰法
3. 隱藏戰法
4. 未展開的 14 人血統矩陣
5. 天命值（若設定為隱藏）
6. 覺醒名號（尚未達成時）
7. 虛擬祖先內容（若玩家尚未解鎖細節）
8. 未公開的稱號來源

### 6.2 遮蔽層級

| 層級 | 意義 | 顯示方式 |
|---|---|---|
| 0 | 已公開 | 完整顯示 |
| 1 | 條件公開 | 灰階或半透明 |
| 2 | 可點擊解鎖 | `🔒 + ? + 提示` |
| 3 | 完全隱藏 | 僅顯示佔位框 |

### 6.3 不想揭露欄位的處理

若資料本身不希望對玩家直接揭露，則：

1. 以 `🔒` 圖示取代數值
2. 顯示欄位類型標籤，不顯示實值
3. 可附帶「尚未解鎖」或「未知」說明
4. 不在 tooltip 中洩露原始數據

---

## 7. 欄位與文案規範

### 7.1 建議文案 key

| Key | 文案 |
|---|---|
| `UI_GENERAL_NAME` | 名稱 |
| `UI_GENERAL_TITLE` | 稱號 |
| `UI_GENERAL_FACTION` | 陣營 |
| `UI_GENERAL_ROLE` | 職能 |
| `UI_GENERAL_AGE` | 年齡 |
| `UI_GENERAL_VITALITY` | 精力 |
| `UI_GENERAL_LIFESPAN_LOCKED` | 天命值：🔒 |
| `UI_GENERAL_EP_RATING` | 爆發力評級 |
| `UI_GENERAL_GENE_LOCKED` | 未知因子 |
| `UI_GENERAL_TACTIC_LOCKED` | 需教官啟發 |
| `UI_GENERAL_TACTIC_UNKNOWN` | 因子不符 |
| `UI_GENERAL_PEDIGREE_EXPAND` | 查看完整血統（3 代 14 人） |
| `UI_GENERAL_APTITUDE` | 戰場適性 |

### 7.2 評語建議

EP 評語可沿用血統理論系統：
- EP ≥ 98：天命之子
- 70~89：良才美質
- 50~69：中規中矩
- < 50：命途多舛

### 7.3 狀態標籤建議

- `Combat`
- `Mentor`
- `Active`
- `Training`
- `Retired`
- `Dead`
- `Hidden`
- `Locked`

---

## 8. 互動與展開規則

### 8.1 血統展開

1. 預設顯示父母兩張卡
2. 點擊「查看完整血統」後展開 14 人矩陣
3. 再次點擊可收合
4. 若資料不足，仍保留骨架位與鎖定標籤

### 8.2 戰法點擊

1. 點擊已習得戰法卡可進詳情
2. 若為可習得戰法，點擊顯示啟發來源
3. 若為不可習得戰法，點擊僅顯示鎖定原因

### 8.3 適性點擊

1. 點擊某項適性可看加成說明
2. 長按可顯示地形或天氣對應效果
3. 移動端預設使用 tooltip 底部抽屜，不要依賴 hover

### 8.4 立繪點擊：血脈面與故事抽屜

1. 點擊角色立繪後，先在人物上方短暫浮現 `命紋靈獸虛影`
2. 接著角色身上的祖紋命篆與符號 slot 依序點亮
3. 日常 premium layout 可直接顯示下方固定故事條；若版面較緊或手機版，再從下方滑出故事抽屜
4. 故事內容預設顯示兩個頁籤：
   - `歷史趣聞`
   - `血脈傳聞`
5. `歷史趣聞` 用來補史實逸事與人物記憶點；`血脈傳聞` 用來合理化其勇猛、執念、魅力或天命來源
6. 故事內容建議採 `5~6 格靜態連環漫畫 + 短文本`，不依賴角色動畫
7. 抽屜或固定故事條都不得遮住角色臉部、胸甲命紋與主要血脈符號 slot

---

## 9. 版面規格建議

### 9.1 桌面版

- 左：頭像 / 人物關鍵資訊
- 中：Tab 內容
- 右：摘要卡 / 快速操作 / 戰法快捷欄
- 建議比例：左側角色主視覺區約 44%~48%，右側內容卡約 36%~40%，最右頁籤欄維持 96~112 px 固定寬度，避免頁籤文字在 Web 與手機橫向模式下被壓扁。
- 摘要卡（HP / SP / 精力 / EP 等）優先靠右上排列，避免壓在立繪底部，維持角色主視覺完整度。
- 若啟用血脈故事抽屜，左側角色主視覺區底部需保留故事浮層空間，但不可讓人物主體寬度低於畫面寬度的 68% 可視佔比

### 9.2 手機版

- 上：頭像與摘要
- 中：Tab 切換
- 下：內容卡垂直排列
- 血統矩陣與戰法列表一律支援折疊
- 手機橫向模式優先沿用「左視覺、右資訊、右側 tab rail」配置，只縮窄左右留白與內容欄寬；直向模式才退化成上下堆疊。
- 頁籤欄文字不可依賴 hover 或超小字硬塞，應允許兩行標籤並保持按鈕高度不少於 84 px。

### 9.3 尺寸建議

1. 觸控目標不小於 44 × 44 px
2. 關鍵卡片最小高度建議 72 px
3. 血統矩陣卡片最小寬度建議 84 px
4. 戰法卡片需保留圖示、名稱、類型、等級四層資訊
5. 角色頁標題建議使用 42~48 px；頁籤文字 14~16 px；內文資訊 18~20 px；次要說明 16~18 px
6. 主要資訊卡內距建議 18~24 px；Tab 內容區左右留白建議 24~32 px；段落間距建議 12~18 px

---

## 10. 資料 Schema 建議

### 10.1 介面用資料模型

```json
{
  "uid": "G001",
  "templateId": "ZY_01",
  "title": "[常山]",
  "name": "趙雲",
  "faction": "Shu",
  "gender": "Male",
  "age": 25,
  "role": "Combat",
  "status": "Active",
  "lifespan": 65,
  "vitality": {
    "current": 100,
    "max": 100
  },
  "sp": {
    "current": 0,
    "max": 100
  },
  "stats": {
    "str": 97,
    "int": 78,
    "lea": 91,
    "pol": 62,
    "cha": 85,
    "luk": 85
  },
  "genes": [
    {
      "type": "Blue",
      "id": "BLUE_STR_05",
      "level": 5,
      "isLocked": false,
      "discoveryLevel": 1
    }
  ],
  "bloodlineId": "ZY_FAMILY",
  "ep": 92,
  "epRating": "天命之子",
  "ancestorsJson": {},
  "learnedTactics": [],
  "inspiredTactics": [],
  "aptitudes": {
    "troop": {},
    "terrain": {},
    "weather": {}
  }
}
```

### 10.2 最小欄位集

若只做最小版人物詳情，至少要有：

1. uid
2. templateId
3. title
4. name
5. faction
6. role
7. status
8. age
9. vitality
10. sp
11. stats
12. skillId
13. preferredTerrain
14. learnedTactics
15. aptitudes

---

## 11. 與現有 `GeneralDetailPanel` 的對接規則

### 11.1 現有實作可沿用的部分

1. `name`
2. `faction`
3. `hp`
4. `sp`
5. `skillId`
6. `attackBonus`
7. `preferredTerrain`
8. `terrainDefenseBonus`

### 11.2 需要擴充的部分

1. 基本資料分頁
2. 血統與因子分頁
3. 戰法與適性分頁
4. 鎖定欄位視覺
5. EP 與評語顯示
6. 14 人血統展開
7. 天氣適性展示
8. 多戰法清單
9. 立繪點擊血脈面提示層
10. `歷史趣聞 / 血脈傳聞` 故事抽屜
11. 英靈虎符 / 血脈卡摘要欄

### 11.2.1 `GeneralDetailBloodlineV3` 對接定位

1. `assets/resources/ui-spec/layouts/general-detail-bloodline-v3-main.json`、`general-detail-bloodline-v3-default.json`、`general-detail-bloodline-v3-screen.json` 屬於 `GeneralDetail` 的 v3 總覽殼層。
2. 這組 v3 ui-spec 不應被視為獨立於 `GeneralDetailPanel` 之外的第二正式人物系統。
3. 後續若整合進 `GeneralDetailPanel.ts`，應以「首頁態升級」為方向，而不是把既有 `TabStats / TabBloodline / TabSkills / TabAptitude / TabExtended` 直接拔除。
4. 若實作上需要暫時保留舊版 `general-detail-main.json` 與 v3 併存，必須明確標示哪一個是正式預設入口、哪一個是過渡態，避免雙母型長期分裂。

### 11.2.2 `GeneralDetail` tab 結構與 `GeneralDetailBloodlineV3` overview slot 對映

| 現行結構 | 目前主要節點 / 資料 | v3 overview 對應 slot | 正式整合策略 |
|---|---|---|---|
| `TabBasics` | `UidValue / NameValue / TitleValue / TemplateValue / FactionValue / GenderValue / AgeValue / RoleValue / StatusValue / LifespanValue / VitalityValue / SourceValue` | `HeaderRow/NameLabel`、`HeaderRow/TitleLabel`、`MetaColumn`、`TopSummaryRow/RoleCard` | `TabBasics` 的核心身分資訊上提到 overview header；較細的身份欄位留在 `Basics` 次層詳情，不必全塞回首頁。 |
| `TabStats` | `StrValue / IntValue / LeaValue / PolValue / ChaValue / LukValue / StatsRoleValue` | `TopSummaryRow/CoreStatsCard`、`TopSummaryRow/RoleCard` | overview 只放六維摘要與定位評語；完整數值列表仍保留在 `Stats` tab。 |
| `TabBloodline` | `BloodlineSummaryCard`、`AncestorTree`、`GeneListCard`、`AwakeningCard` | `BloodlineRow/BloodlineSummaryCard`、`BloodlineSummaryFields`、`BloodlineCrestCard`、`BloodlineCrest` | overview 只承接血脈名稱、覺醒進度、性格摘要與 crest；`14 人矩陣 / gene list / 展開樹` 仍屬 `Bloodline` tab 深度內容。 |
| `TabSkills` | `PrimarySkillCard / LearnedSkillsCard / InspiredSkillsCard / LockedSkillsCard / SkillNoteCard` | `TopSummaryRow/TraitCard` 的戰鬥特性摘要 | overview 只顯示一級戰鬥定位與代表戰法提示；技能清單與可點擊條目仍留在 `Skills` tab。 |
| `TabAptitude` | `TroopCard / TerrainCard / WeatherCard / TerrainSummaryCard` | `TopSummaryRow/TraitCard` 的適性摘要 | overview 只保留兵種 / 地形偏好摘要；完整適性矩陣仍留在 `Aptitude` tab。 |
| `TabExtended` | `CurrentHpValue / CurrentSpValue / AttackBonusValue / HiddenFlagsValue / NotesValue / DevNoteValue` | `UnownedPortraitRibbon / UnownedPortraitHint / UnownedInfoBadge` 等狀態提示 | `Extended` 多屬營運或開發補充欄；除持有狀態與少量提示外，不應回灌到首頁主要視覺。 |
| overview-only 故事層 | 舊版 tab 無對應正式欄位 | `StoryStripRail / StoryStrip / StoryCell01~06` | 這是 `日常人物頁 v3` 新增的首頁層能力，需以故事資料或 narrative slot 另建資料來源，不應硬塞進既有 tab 文本欄位。 |

### 11.2.3 分頁架構對 `日常人物頁 v3` 母型的影響評估

1. 分頁機制本身不衝突；衝突點在於若把 `GeneralDetailBloodlineV3` 硬塞進舊 `RightContentArea/TabBasics`，會直接壓縮 v3 需要的 `左人物 + 右大資訊卡 + 下方故事條` 比例。
2. 舊版 `general-detail-main.json` 的 `RightContentArea` 只有右側窄欄內容區，而 v3 是整體首頁殼層；因此 v3 應升級為 `GeneralDetail` 的外層首頁 layout，而不是 `TabBasics` 內的一張子卡。
3. `RightTabBar` 仍可保留為正式導航，但應視為外層 chrome；若保持 96~112 px 常駐寬度，需在 desktop 與 mobile 驗證是否擠壓 v3 資訊卡與故事條。
4. 對目前已收斂的 `日常人物頁` UI 結構來說，真正需要保護的是三件事：人物仍是第一主角、血脈只是深層人格區、故事條是首頁固定層；只要這三件事不被分頁 chrome 吃掉，分頁架構就可以共存。

### 11.2.4 其他 tab 的設計策略與文件維護原則

1. 其他 tab 預設不另開一張全新正式人物頁，而是沿用同一個 `GeneralDetail` 母型，在同一套外框下切換內容模組。
2. `Basics / Stats / Bloodline` 屬於與 overview 高度連動的內容，應優先做成同 family 的模組化延展，不必先重設一套新美術母板。
3. `Skills / Aptitude / Extended` 若只是資訊密度較高，可先以同母型下的次層卡片、抽屜或捲動模組處理；只有互動模型明顯改變時，才考慮補一張專屬子規格圖。
4. 後續若要排程開發，建議至少拆成三類任務卡：`overview 首頁整合`、`bloodline 深度內容整合`、`skills/aptitude/extended 模組化整理`。
5. 文件維護上，正式欄位與互動若有新增，優先回寫本規格書；若涉及整體視覺母型與 family 規則，再同步回寫 `docs/UI 規格書.md`，不可再只停在補遺。
6. 工程過渡期允許先以 `overview shell host + tab content host` 的雙層結構實作；其中 overview shell 可先獨立掛載、驗證 slot 與資料映射，再併回 `GeneralDetailPanel` 外層，不視為第二正式人物系統。
7. 過渡整合期允許先讓 `Basics` 分頁路由到 overview shell，而 `Stats / Bloodline / Skills / Aptitude / Extended` 繼續沿用既有 tab content host；待外層 host 收斂完成後，再決定 `Basics` 是否保留為次層詳情頁。

### 11.3 重構原則

1. 不直接把所有資料灌進單一長頁
2. 以分頁或折疊群組降低認知負擔
3. 讓資料載入與 UI 呈現分離
4. 鎖定欄位只做視覺表現，不在 View 中推導遊戲邏輯

---

### 11.2.5 `GeneralDetailBloodlineV3` 第一批 slot family / skin 收斂原則

1. `GeneralDetailBloodlineV3` 第一批收斂優先順序為：`slot family / skin contract -> skeleton node contract -> story / bloodline data contract`，不得把這三件事混成同一輪半做。
2. 視覺收斂第一步優先重用既有 `general_detail` family 資產，包含 `detail_content_bg`、`detail_summary_card_bg`、`detail_section_bg`、`detail_footer_bg` 等共用 frame / fill / rail，而不是繼續擴張 `color-rect` placeholder。
3. `日常人物頁 v3` overview 殼層至少必須先固定下列 slot family：
   - `portrait`：人物載體、未持有提示
   - `info.card`：右側外層資訊殼
   - `summary.card`：核心能力 / 角色定位 / 性格摘要
   - `bloodline`：血脈摘要主卡、命紋徽記承載區
   - `story.strip`：底部故事帶 rail 與 cell family
4. 在 `UI-2-0058` 完成前，不應直接大改 `GeneralDetailBloodlineV3` 節點結構；`UI-2-0059` 應承接已固定的 slot family，再做 node contract / runtime 收斂。
5. 在 `UI-2-0059` 完成前，不應把暫時文案視為正式內容來源；`UI-2-0061` 應承接正式 node / slot 結構後，再收斂故事帶、血脈摘要與趣聞欄位契約。
6. `overview shell` 的卡片文案、故事帶文字與血脈摘要節點，應直接宣告在 `layout JSON` 中；runtime 層只負責填值與切換狀態，不應再以臨時建立 `Label` 節點的方式長出最終畫面。

## 12. 目前落地狀態

### 12.1 已接通的實作檔案

1. `assets/scripts/core/models/GeneralUnit.ts`：共享 `GeneralConfig` 與武將資料模型
2. `assets/scripts/ui/components/GeneralDetailPanel.ts`：分頁式武將詳情面板
3. `assets/scripts/ui/components/GeneralListPanel.ts`：武將列表來源
4. `assets/scripts/ui/scenes/LobbyScene.ts`：列表與詳情的入口場景
5. `assets/resources/data/generals.json`：Demo 武將資料入口
6. `assets/resources/ui-spec/layouts/general-detail-main.json`：分頁版版面草案
7. `assets/resources/ui-spec/skins/general-detail-default.json`：分頁版樣式草案
8. `assets/resources/ui-spec/screens/general-detail-screen.json`：詳情頁組裝契約

### 12.2 目前顯示規則

1. 未提供欄位以 `🔒` 或折疊區塊顯示
2. 已知欄位可直接顯示，不再要求先補齊所有系統資料
3. 目前頁籤包含基本資料、六色屬性、血統與因子、戰法與技能、戰場適性、延伸資訊
4. 後續若接入正式血統矩陣或戰法資料，只需擴充 `GeneralConfig` 選填欄位，不需改動主流程

### 12.3 美術接線文件

1. 節點名稱、圖層與 icon 命名以 [武將人物介面美術接線清單.md](武將人物介面美術接線清單.md) 為準
2. 若新增頁籤或替換 icon，必須同步更新該接線文件與 `general-detail-main.json`
3. 美術端可先依接線文件產出 icon 與卡片素材，不必等待程式再改節點命名

---

## 12. 驗收標準

### 12.1 基本驗收

1. 武將人物介面可正確顯示基礎欄位
2. 六色屬性完整顯示
3. 血統分頁可切換與展開
4. 戰法可分三態顯示
5. 適性矩陣可顯示 S~D 級別
6. 鎖定欄位確實以 `🔒` 或 `?` 表示

### 12.2 正式版驗收

1. 可容納完整 14 人矩陣
2. 可顯示至少 3~5 個因子槽
3. 可顯示戰法等級與 TP 提示
4. 可在手機尺寸下正常閱讀
5. 與 `GeneralUnit` 資料可穩定對映

---

## 13. 實作備註

1. 這份規格應作為 `GeneralDetailPanel` 的設計母規格
2. 若未來需要再拆頁，可保留 header / summary strip / tab bar 的骨架不變
3. 所有新增欄位都要先回填到 cross-reference index
4. 若新增設計決策，請同步更新 `docs/keep.md`

---

## 14. 總結

武將人物介面不是單一角色卡，而是玩家理解一名武將完整生命資料的入口。它應該同時承載：

1. 身份
2. 數值
3. 血統
4. 戰法
5. 適性
6. 隱藏與解鎖狀態

本規格的核心精神是：
- 已知就清楚顯示
- 未知就鎖起來
- 深度資訊可展開
- 正式版欄位先預留完整骨架
