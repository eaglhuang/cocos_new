---
id: UI-2-0092
priority: P1
phase: G
created: 2026-04-05
created_by_agent: Agent2
owner: Agent1
status: open
type: asset-generation
related_cards:
  - UI-2-0054
  - UI-2-0087
  - UI-2-0089
  - UI-2-0091
depends:
  - UI-2-0089
  - UI-2-0091
template_family: detail-split
content_contract:
  - rarityTier
  - crestState
  - crestFaceAsset
skin_fragments:
  - general_detail/jade-parchment-panel-final
  - general_detail/crest-medallion-final
  - general_detail/jade-rarity-badge-final
smoke_route: "node tools_node/capture-ui-screens.js --target GeneralDetailOverview --outDir artifacts/ui-qa/UI-2-0092"
docs_backwritten:
  - docs/keep.md
  - docs/UI 規格書.md
  - docs/cross-reference-index.md
notes: "2026-04-05 | 狀態: open | 驗證: pending | 變更: 開立 Agent1 正式切圖子卡，第一批只做 jade panel / crest medallion / rarity badge 三個高重用 family | 阻塞: 等 UI-2-0089 / UI-2-0091 收斂 art recipe 與 asset direction"
---

# UI-2-0092 Jade / Crest / Rarity 第一批正式切圖

## 目標

由 Agent1 產出 `GeneralDetailBloodlineV3` 第一批可重用正式資產，不再停留在 proof crop 或近似拼裝版。這張卡只負責高重用 family，不直接擴散到整頁所有素材。

## 範圍

第一批正式 family 僅包含三項：

1. `jade-parchment-panel-final`
2. `crest-medallion-final`
3. `jade-rarity-badge-final`

## 交付物

- `jade-parchment-panel-final`
  - `header band`
  - `body frame`
  - `cap / corner / noise / inner shadow` 可分層切圖
- `crest-medallion-final`
  - `medallion ring`
  - `medallion face`
  - `glyph safe area`
  - `rarity tint` 可疊加
- `jade-rarity-badge-final`
  - `common / rare / epic / legendary` 至少四階
  - 與 `jade-parchment` family 同語言，不可回到獨立紫色風格

## 驗證重點

- [ ] 不可再引用 `proof/` 目錄素材當正式輸出
- [ ] `jade header` 與 `crest medallion` 必須屬於同一套視覺 family
- [ ] `rarity badge` 必須與 panel / crest 不打架
- [ ] 套入 `GeneralDetailOverview` 後，右側主框與右下 crest 不可再出現明顯黑塊 proof 感
- [ ] `node tools_node/capture-ui-screens.js --target GeneralDetailOverview --outDir artifacts/ui-qa/UI-2-0092` 可產出 compare 用截圖

## 分工說明

- Agent1：正式資產生產與切圖品質
- Agent2：art recipe / asset direction / compare board / runtime 掛載驗證

## 備註

- 這張卡是 `UI-2-0089` 與 `UI-2-0091` 的執行子卡，不可跳過 recipe 與 direction 直接擴張整頁切圖。
- 若第一批正式 family 驗證通過，後續再擴到 `portrait-stage-final` 與 `story-strip-final`。
