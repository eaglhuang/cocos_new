---
doc_id: doc_task_0102
id: UI-2-0079
priority: P3
phase: E
created: 2026-04-05
created_by_agent: Agent1
owner: Agent1
status: open
type: chore
related_cards:
  - UI-2-0074
depends: []
notes: "2026-04-05 | 狀態: open | 驗證: 確認 Constants.ts 仍存有 @deprecated UILayerName enum | 變更: 開單清理 | 阻塞: 建議等 UI-2-0074 完成後執行，確認無殘留呼叫端"
---

# [UI-2-0079] Constants.ts UILayerName deprecated 清理

## 開單原因

`Constants.ts` 仍存有 `@deprecated M-1 重構後改用 LayerType` 標記的 `UILayerName` enum，與 `UIConfig.ts` 的 `LayerType` 並存，對新加入 Agent 或人員造成語義混淆（兩個表示層級的 enum 哪一個才是現行標準？）。

## 完整描述

1. grep `UILayerName` 確認無任何呼叫端使用（預期 0 個使用點）
2. 從 `Constants.ts` 移除 `UILayerName` enum 完整定義
3. 確認 `LayerType`（在 `UIConfig.ts`）是唯一殘留的層級 enum
4. 可選：在 `Constants.ts` 加入指引注釋，說明層級相關定義已移至 `UIConfig.ts`

## 來源依據

- `docs/架構評估報告_2026-04-05_Agent1.md (doc_tech_0001)` (doc_tech_0001) 問題 A7
- `assets/scripts/core/constants/Constants.ts`（含 deprecated UILayerName）
- `assets/scripts/core/config/UIConfig.ts`（含現行 LayerType）

## 如何驗證

1. grep `UILayerName` → 0 命中（移除後無任何引用）
2. `Constants.ts` 不再 export `UILayerName`
3. TypeScript 編譯無錯誤
4. `run-acceptance.js` 全綠
