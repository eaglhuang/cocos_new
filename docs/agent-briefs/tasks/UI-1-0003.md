---
id: UI-1-0003
priority: P0
owner: Agent2
status: not-started
type: sprite-move
created: 2026-03-31
related_cards: []
notes: ""
---

# [UI-1-0003] 搬遷 `warning` 按鈕 Family 視覺資產

## 基本資訊
| 欄位 | 值 |
|---|---|
| 卡號 | UI-1-0003 |
| 優先級 | P0 |
| 開單時間 | 2026-03-31 |
| 負責 Agent | Agent2 |
| 狀態 | not-started |
| 完成度 | 0% |
| 完成時間 | — |
| 關聯卡號 | — (接線任務卡待開立) |

## 開單原因
刪除、重置、拆解等不可逆操作畫面需要 `warning` family（紅棕 / 深紅 / 焦褐高對比），目前 runtime 路徑尚未建立。此 family 為未來防止誤操作的視覺安全層。

## 完整描述
- **來源**：`artifacts/ui-layered-frames/family-previews/warning/`
- **目標**：`assets/resources/sprites/ui_families/common/warning/`
- **搬遷檔案**：`btn_primary_normal.png`、`btn_primary_pressed.png`、`btn_primary_disabled.png`
- 搬遷後計算 SHA256 與大小，登記至 `notes` 與 manifest。

## 來源依據文件索引
- `artifacts/ui-source/button-family-screen-audit-2026-03-31.csv`（審計主表）
- `docs/keep.md` §4.1 按鈕家族分流規範（warning.destructive 定義）
- `docs/agent-briefs/Readme.md`
- `docs/系統規格書/名詞定義文件.md`

## 如何驗證
1. 目標路徑下三個 PNG 存在且大小 > 0。
2. 來源與目標每個檔案 SHA256 一致。
3. manifest `UI-1-0003` 的 `status` == `done`，`notes` 含檔案清單。

## Unit Test 建議
**不需要**（搬遷任務以 SHA256 比對作為驗收）。

## 建議作法
```powershell
$src = "artifacts/ui-layered-frames/family-previews/warning"
$dst = "assets/resources/sprites/ui_families/common/warning"
New-Item -ItemType Directory -Force -Path $dst
foreach ($f in @('btn_primary_normal.png','btn_primary_pressed.png','btn_primary_disabled.png')) {
  Copy-Item (Join-Path $src $f) (Join-Path $dst $f) -Force
  $hash = (Get-FileHash (Join-Path $dst $f) -Algorithm SHA256).Hash
  $size = (Get-Item (Join-Path $dst $f)).Length
  "$dst/$f | $hash | $size bytes"
}
```

## 相關聯任務卡
- [UI-1-0001](UI-1-0001.md)、[UI-1-0002](UI-1-0002.md)、[UI-1-0004](UI-1-0004.md) — 同屬 UI-1 子系統，可並行執行
- 接線任務卡（UI-2-xxxx）待後續開立
