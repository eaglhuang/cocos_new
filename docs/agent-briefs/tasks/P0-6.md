---
id: P0-6
status: deprecated
replaced_by: UI-2-0006
---

> ⚠️ **此任務卡已廢棄，請改用新卡號。**
>
> 新卡號：[UI-2-0006](UI-2-0006.md)

# P0-6 — 驗證共用按鈕 border 20px 規範（Agent1）

短述
- 驗證 `nav_ink` / `paper_utility` / `warning` 三組 shared button family 在 skin JSON 中皆使用 `[20,20,20,20]` 的九宮格 border，且不會與既有按鈕高度衝突。

步驟
1. 搜尋引用 shared button family 的 skin slot，確認 `border` 統一為 `[20,20,20,20]`。
2. 以驗證腳本或 acceptance 流程確認高度與 9-slice 規範未衝突。
3. 更新 manifest 與本檔 frontmatter 的 `status` 與 `notes`。

驗收準則
- shared button family 的引用點皆通過 border 驗證
- `docs/ui-quality-todo.json` 中對應 `UI-2-0006` 任務標示為 `done` 並含 notes
