---
id: P0-2
status: deprecated
replaced_by: UI-2-0001
---

> ⚠️ **此任務卡已廢棄，請改用新卡號。**
>
> 新卡號：[UI-2-0001](UI-2-0001.md)

# P0-2 — 更新 skin 指向（Agent1）

短述
- 當 `P0-1` 完成後，將 `lobby-main-default` 的 button-skin 指向 `nav_ink` family 並驗證 9-slice border 與 JSON schema。

步驟
1. 確認 `P0-1` 已完成且目標 sprite 路徑存在。
2. 在 `assets/resources/ui-spec/skins/` 中找到 `lobby-main-default` 的 skin JSON，更新對應 `button-skin` 欄位指向 `assets/resources/sprites/ui_families/common/nav_ink/btn_primary_normal.png`（或相應的 SpriteFrame path）。
3. 驗證該 skin 的 `border` 為 `[20,20,20,20]`（按鈕規範）。
4. 執行 JSON schema 檢查（本專案工具或 script）並產出 diff / 截圖作為交付物。
5. 更新本檔 frontmatter 與 `docs/ui-quality-todo.json` 的 `status` 為 `done`，在 `notes` 中填入被更新的 JSON 路徑與簡短說明。

驗收準則
- 更新後的 JSON 通過 schema 檢驗
- 引用的 sprite 路徑存在
- `docs/ui-quality-todo.json` 中 `P0-2` 標示為 `done` 並包含 notes
